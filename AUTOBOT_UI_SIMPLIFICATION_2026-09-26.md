# Упрощение интерфейса Автобота — 26 сентября

Исходная версия AutoBot `8767d05`, CRM `1e22176`. До работы tracked CRM diff пустой; в AutoBot уже изменён `data/search_resume_checkpoint.json`. Этот файл и пользовательские `.impeccable/`, `.agents/`, `.codex/`, outputs сохранены. Текущий пакет — интерфейс смет, тендеров, поиска и оболочки CRM.

## Результат

Одна навигация «Сметы / Тендеры / База цен / Поиск позиции» на всех основных экранах. Убраны рекламный слоган, дубли заголовков/сводок и отдельная повторная кнопка результатов поиска над таблицей. Состояние и результаты доступны во вкладке поиска. Список смет показывает сумму, позиции и состояние каждой сметы; кнопка добавления в объект появляется при выборе строк.

Дополнительные фильтры, сведения о закупке и действия выбора строк раскрываются по нажатию. Настройка поиска в смете изначально свёрнута; существующий механизм раскрывает её при запуске/ошибке. Важная сверка источника при расхождении остаётся раскрытой. Убраны лишние тени, градиенты, вложенное оформление и завышенные размеры цифр. Оболочка CRM стала компактнее; загрузка, обновление и переход в отдельное окно сохранены.

Существующие URL/API, загрузка/проверка/выгрузки, фильтры, суммы, импорт CRM и подтверждение отправок не менялись. Данных об использовании для удаления целых функций нет: удалены дубли представления, а возможности сохранены. Параллельная задача поставщиков владеет отдельной вкладкой «Поставщики», buyer UI/API, фото и перепиской; её изменения выпускаются своим коммитом.

## Проверка

На изолированной копии, с отключённым внешним соединением:

- Python: **58 passed, 1 skipped** — `test_estimate_workspace.py`, `test_tenders_quiet_ui.py`, `test_web_ui_delivery.py`, `test_tender_detail_filters.py`, `test_tender_detail_market.py`, `test_supplier_catalog_api.py`, `test_web_ui_downloads.py`, `test_web_ui_scope_notice.py`, `test_tender_navigation_loader.py`, `test_estimate_upload_progress.py`.
- Node: PASS — `tender_workspace_frontend_tests.js`, `tender_price_filters_frontend_tests.js`, `tender_market_run_frontend_tests.js`, `estimate_workspace_frontend_tests.js`, `estimate_upload_frontend_tests.js`, `research_frontend_tests.js`, `supplier_catalog_frontend_tests.js`; CRM `autobot_workspace_frontend_tests.js` PASS.
- Браузер 390/768/1280 px: список и карточка смет, список и карточка тендеров, вкладка поиска, база цен, поиск позиции и оболочка CRM. Документ не переполняется по горизонтали; таблицы и вкладки имеют собственную прокрутку. Проверены длинное название тендера, выбор смет/появление импорта, поиск/пустое состояние/сброс, раскрытие дополнительных действий, выбор/снятие 20 строк, переход в поиск и reload с `#search`. CRM загружает iframe, статус и иконки действий видны.
- В каталоге preview две локальные сметы, в тендере — копия production на 259 строк. Не все вложения preview скопировались из-за несовместимых Windows имён; оригинальные файлы проверяются отдельными тестами и при выпуске. Переписка/отправка и полный ввод данных не запускались в production.
- Три ранних падения относились к старому декоративному markup навигации и удалённой дублирующей кнопке. Ожидания заменены проверкой реальных ссылок, выбранного раздела и доступной вкладки поиска. Полные CRM/AutoBot suites не запускались. Самопроверка diff не является независимым ревью.

Артефакты: `tmp/autobot-minimal-20260926` в CRM: исходные diff, изолированный runtime, логи тестов, `visual-final.json`, снимки `final-*.png`. Данные preview не входят в Git и не переносятся в production.

## Публикация и итог

Опубликованы AutoBot `a511b0e956441a689c2629ae1d3a30040975842b` и CRM `dddb826292dc988bf4d93d78f71fbbe03ede9271`; код интерфейса сначала `da4a9ff` / `e4f509d`, отдельный пакет поставщиков `342d72d`. Финальные Markdown-коммиты переносятся fast-forward без перезапуска.

В первом опубликованном варианте браузер обнаружил, что существующий Nginx пропускает только известные имена `/static/*.css`: новая навигация осталась без стилей. Исправлено отдельным коммитом `a511b0e`: CSS отдаётся через существующий защищённый `/estimates/workspace-nav.css`. Nginx и правила доступа не расширялись. Добавлена проверка доставки CSS; после исправления 23 затронутых Python-теста PASS. Настоящая CRM в браузере при 1280/390 загрузила оформленную навигацию и 5 смет; мобильный iframe scrollWidth = clientWidth = 340, переход к тендерам работает.

Linux-кандидат без рабочей БД, секретов и сети: шаблоны, пустой каталог, assets и копеечное сравнение PASS; **140 buyer/hermes unittest PASS**, в том числе с предварительным импортом настоящего web_ui. Первый объединённый smoke дал 139/140 из-за теста, подменявшего sys.modules без уже загруженного атрибута пакета. Fixture исправлен в `310978a`; production-логика не менялась. Пропущенный локальный тест связан с недоступным созданием symlink на Windows. Полные CRM/AutoBot suites не запускались.

Проверка публикации: совпали 4 изменённых CRM-файла и 20 AutoBot-файлов внутри контейнеров, CSS/JS HTTP-ответы, 5 карточек смет, 3 валидные Excel-выгрузки. 7 защищённых маршрутов, включая новый CSS и image endpoint, отклоняют запрос без входа. Health, HTTPS, SQLite integrity и прежние FK-показатели сохранены.

Закрытый резерв `/opt/crm-backups/autobot-minimal-20260926T134540Z`: 5 CRM и 13 AutoBot SQLite через online backup; integrity и восстановление проверены. В CRM сохранены прежние 3 нарушения FK, в AutoBot — 0. Первая попытка архива столкнулась с исчезающим служебным -shm; companions исключены до stat, повторный полный резерв проверен. Исходные rollback-теги: `crm-crm:rollback-1e22176fbfe0`, `crm-autobot:rollback-8767d05ad09b`; перед вторым build дополнительно сохранены `rollback-dddb826292dc` / `rollback-342d72d9964c`.

Перед каждым переключением активных market/upload/buyer/search операций не было. После финального переключения все таблицы `buyer_jobs.sqlite3`, `buyer_outbox.sqlite3`, `agent_market_jobs.sqlite3` совпали по counts и SHA256 содержимого строк с activation-baseline. Сохранены 33 completed buyer jobs; исходящие blocked 1 / sent 2 / uncertain 2; inbox blocked 2; market canceled 142 / completed 661 / failed 40. Исходная смета «Волги» и Nginx не изменились. Письма и новые поиски при UI QA не запускались.

Production manifest: `/opt/crm-releases/a511b0e956441a689c2629ae1d3a30040975842b/verified-ui.json`; baseline с hashes каждой таблицы находится рядом в `activation-baseline.json`. Локальные логи `fix_smoke.log`, `fix_activate.log`, `fix_verify.log`, снимки `production-crm-final-*.png` — в каталоге артефактов задачи.

Порученный пакет выполнен. Для удаления целых функций нужен отдельный разбор реального использования; в этом выпуске убраны дубли и оформление, а существующие возможности сохранены.
