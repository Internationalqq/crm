# Вход по почте — 23 сентября 2026

Baseline CRM `2719827d6d09c36e8e9018d944eee8f595a6d5d5`, tracked diff пустой, `.agents/` и `.codex/` сохраняются. Production совпадает. Clerk выключен, Resend настроен; две активные записи с email, повторяющихся активных адресов нет. Адреса и секреты в отчёт не выводятся.

Результат: на `/login` пользователь выбирает «Войти по почте», вводит один адрес, затем шестизначный код. Повтор через 60 секунд с серверным ограничением; изменение адреса, вставка целого кода, ошибки, истечение, состояние отправки и восстановление после обновления страницы. Светлое оформление существующего входа, обычный пароль и восстановление сохраняются. Новый сценарий — Operate, расширение существующей поверхности без нового визуального мира.

Код действует 10 минут, не хранится открытым, не логируется, привязан к случайному запросу, используется атомарно один раз. Пять попыток на код, ограничения отправок на email/IP и проверок на IP хранятся в SQLite и переживают перезапуск. Повтор аннулирует прежний код. Только существующий однозначный активный аккаунт; при смене email/блокировке вход запрещён. Роли, создание пользователей и пароли не меняются. Ответ запроса не раскрывает существование учётной записи; ошибки доставки аннулируют код и фиксируются без кода/токена. При Clerk новый локальный способ отключён, чтобы не обходить политику провайдера.

Проверки: API успех/ошибка/повтор/срок/лимиты/конкурентное погашение/деактивация/смена email/дубликаты/почтовая ошибка, сохранение пароля и запрета cross-site. Изолированная копия проекта и БД, подставной почтовый транспорт. UI на 390/768/1280: шаги, клавиатура, вставка, таймер, повтор, ошибки, перезагрузка и прежний вход. Реальная доставка проверяется только на явно предоставленный пользователем тестовый адрес; вопрос отправлен, реализацию не блокирует.

Миграция: две новые служебные таблицы одноразовых запросов и попыток проверки с индексами; существующие записи не преобразуются. До production — приватный резерв БД/кода/образа, пробный запуск новой версии на копии и сравнение прежних таблиц. Инициализация повторяема. При откате вернуть старый образ, новые служебные таблицы можно оставить: старый код их не использует; рабочую БД не затирать. После выпуска проверить маршруты и запреты, вход/повторное открытие, сохранность данных и неизменность AutoBot.

Артефакты: `tmp/email-login-20260923`. Реализация и проверки в работе.

## Проверено до выпуска

17 тестов одноразового входа, 23 существующих теста авторизации, 7 HTTP-security и 19 guest-access — PASS на изолированной копии. `node tests/login_entry_frontend_tests.js` — PASS. Browser E2E на локальной копии: email → запрос → ошибка → правильный код → CRM, cookie/`me`, cross-site 403, пароль, восстановление pending-запроса после reload. 390/768/1280 без горизонтального переполнения и JS-ошибок. Detector выполнен один раз; замечания к общим старым стилям/системе презентации не распространялись на весь CRM. Свежий reviewer: `fix` для выбранного способа входа, после исправления — `ship` по этому замечанию. Документация: `EMAIL_LOGIN_DESIGN.md`.

Приватный проверенный резерв: `/opt/crm-backups/email-login-20260923T073252Z`; restore/integrity OK. Исходное нарушение FK (1) сохраняется, не исправляется удалением данных. Подготовлен rollback-tag `crm-crm:rollback-email-login-2719827d6d09`. Испытание Linux-образа и миграции на копии, публикация и проверка внешнего домена — следующий шаг. Реальный тестовый email пользователь пока не предоставил.

## Выпуск и итог

Реализация `9e61be933db0615c427c6382b88132063c159fbe` опубликована 23 сентября. Образ `sha256:4f46dfd0be95eb8d22d951e1cf511a658dfd98f38d24acac8d7461a4d717761b`. GitHub main и рабочий сервер синхронизированы. CRM перезапущена; образ и время запуска AutoBot сохранены, nginx SHA256 не изменился (`56b5422f0eeb465be0a348108d085f7922eb949bdaa48915896d001eee2109b8`).

В новом Linux-образе без сети и рабочей БД: **70 тестов PASS** в семи наборах (`test_auth_passwords`, `test_auth_security_hardening`, `test_auth_request_cache`, `test_email_login`, `test_http_request_security`, `test_guest_access`, `test_http_static_delivery`). Node login и guest-access PASS. Это затронутые наборы, не вся тестовая база CRM.

Испытание миграции на приватной копии: два вызова `ensure_schema`, два новых служебных объекта-таблицы, исходные строки всех таблиц совпали по отпечаткам. Первая более строгая проверка полного startup остановилась на таблице sessions: старый код `init_db` удаляет истёкшие сессии. После проверки причины отдельный trial доказал, что удаляется ровно одна уже просроченная сессия, **все действующие сессии и все рабочие таблицы неизменны**. Код ради прохождения проверки не менялся. Integrity OK, исходное нарушение FK осталось одним; автоматического исправления связей нет.

Браузер на изолированной копии: реальная минута до разблокировки повторной отправки, новый challenge, истечение, длинный адрес на 390 px, offline/повтор, восстановление пароля отдельным переходом. Первый расширенный UI-прогон неверно моделировал открытие email-ссылки сменой hash уже загруженной страницы; после настоящего нового перехода reset-проверка PASS. После обновления пароля старая незавершённая попытка email-входа очищается; отдельная UI-проверка с подставным ответом reset PASS.

Публичный HTTPS: `/login` 200 и `emailLoginEnabled=true`; четыре изменённых JS/CSS совпадают с релизом. Неверный адрес/формат кода — 400, cross-site — 403, анонимный `/api/users` — 401. Production browser: переключение email/пароль, одно поле адреса, восстановление пароля доступно, 390/768/1280 без горизонтального переполнения и JS-ошибок. Боевые тестовые учётные записи и коды не создавались, письма реальным адресатам не отправлялись.

Ограничение: реальная доставка в почтовый ящик не подтверждена. Используется уже настроенный Resend; пользовательский тестовый адрес ещё не предоставлен. Следующий шаг — проверка получения письма на указанной пользователем почте действующей учётной записи. Полная регрессия всей CRM и другие почтовые клиенты/мобильные ОС не проверялись. Изолированный preview завершён; артефакты и протоколы сохранены в `tmp/email-login-20260923`.
