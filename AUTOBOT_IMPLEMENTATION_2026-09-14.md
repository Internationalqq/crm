# Автобот — журнал реализации

Пользователь одобрил реализацию 14 сентября 2026. Основной документ — [план](AUTOBOT_REFACTOR_PLAN.md).

## Исходное состояние и границы

- CRM: `4a2b14968a364947a78a296af9ed9d81efeaf2e2`; ранее подготовленные план/аудит и ссылки в двух документах сохранены.
- Локальный Автобот был на `4bddc07`, production и GitHub — на `3f548de738240f93fddfe4dc8d35b09bf51ab1ea`. Опубликованное исправление импорта ПК РИК включено в изолированную копию и проверено. Локальная ветка обновлена fast-forward до этого commit.
- Исходный изменённый `data/search_resume_checkpoint.json` не относится к выпуску и сохраняется. Исходный SHA-256: `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8`.
- Текущий сервер: `193.187.94.165`, CRM `/opt/crm`, Автобот `/opt/code/auto_bot`. CRM совпадала с локальным HEAD, AutoBot `/healthz` отвечал. В серверном checkout Автобота найден прежний untracked backup `data/migration-20260905-agent_market_jobs.sqlite3`; он сохраняется.
- Эксперименты и preview выполняются в `tmp/autobot-refactor-20260914`, без секретов и с запрещённой исходящей сетью. Рабочие базы и исходные сметы не мигрируются.

## A1 — опубликовано

1. Общие правила подтверждения цен и однозначного сопоставления используются в merge, карточке тендера, своих сметах и HTML-отчётах. Кандидаты, старые счётчики и числа из текстов не становятся расчётными ценами.
2. Одинаковые названия с разными единицами, файлами, координатами или изменёнными значениями не объединяются. Неоднозначное соответствие требует нового поиска.
3. Полнота считается по числу позиций и сумме распознанных строк. Неизвестные суммы сохраняются как неизвестные; разница со сметой отделена от прибыли. Частичный результат не выдаёт положительного заключения о тендере.
4. Рыночные итоги округляются до копейки на уровне строки. Исправлен повторный множитель нормативного блока в итоговой стоимости.
5. CRM принимает расчётную цену только из отчёта с новым признаком проверенного контракта, сохраняет медиану вместо выбора минимума; сопоставляет единицу и индекс и не назначает одну цену двум владельцам.
6. Повтор поиска сохраняет результаты соседних одноимённых строк. Пустые/кандидатные результаты не считаются завершёнными. Поиск сначала берёт дорогостоящие строки, запись XLSX выполняется через атомарную замену.
7. Новые задания исполнителя связаны с отпечатком исходной позиции. Изменённые входные данные и неоднозначные старые задания отклоняются; копирование результата на эквивалентные строки ограничено.
8. В карточке сверху показаны полнота и непроверенная сумма; расширенные инструменты свернуты, основной запуск продолжает поиск недостающих цен. Сохранены источники, фильтры, документы, выгрузки и импорт. Уточнены подписи без названий внутренней установки исполнителя.
9. Лимит поиска применяется после исключения строк с подтверждёнными ценами. В списке своих смет считается число позиций с принятой ценой, а не число сохранённых строк или обработанных заданий. Выгрузка сравнения строится по текущим входным данным; старые числовые поля не проходят в неё без доказательств.
10. Сохранённый источник без единицы остаётся доступен как кандидат. В собственных сметах также показаны полнота по стоимости и сумма без подтверждённой цены. Очередь отличает ожидание исполнителя от взятого задания; наличие API больше не обещает запуск на Mac mini.

## Проверки до выпуска

- Автобот на изолированной копии: **245 tests + 20 subtests passed** после подключения опубликованного исправления ПК РИК. Последний лог: `tmp/autobot-refactor-20260914/pytest-last.log`.
- CRM, полный прогон: 456 Python-проверок, одно падение в фикстуре без единицы (`test_market_snapshot_persists_price_source_date_and_estimate_version`); остальные прошли. Все **49 frontend-наборов прошли**. Логи: `tmp/checks/20260914-203211-e25cab`.
- Фикстура дополнена единицей по действующему контракту; затронутый `test_market_analysis_prices.py` после исправления прошёл (`tmp/checks/20260914-204052-59e586`). Ранее прошёл целевой набор `test_autobot*.py`.
- Повторный **полный прогон CRM прошёл: 456 Python-проверок и 49 frontend-наборов**, `tmp/checks/20260914-211954-139e4c`. Изменение тестовой фикстуры сохранило проверку записи источника, даты и версии сметы.
- Карточка на сохранённом тендере `0171200001926000664`: проверены 390/768/1280 px, горизонтальное переполнение страницы отсутствует. Проверены раскрытие действий, видимость выгрузки/импорта, переход клавиатурой к первому действию, фильтр подтверждённых цен, пустой поиск и очистка. Раскрытие источника сохраняет доказательство, ссылку на оригинал и аудиторский снимок; Escape закрывает панель. Проверены список собственных смет и открытие сохранённой сметы. Документы исходного тендера в preview не копировались, поэтому показано пустое состояние файлов.
- Просмотр diff выполнен самим автором: входные суммы сохранены, кандидаты остаются видимыми, публичные URL сохранены, серверные права CRM не менялись, чужой checkpoint не включён. Независимое ревью не проводилось.

## Не закрыто

A0: в рабочей очереди 141 отменённое, 28 завершённых и 40 неуспешных заданий; активных нет. Последнее обновление с идентификатором исполнителя — `2026-08-24T07:06:24Z`. Это не доказывает его текущую доступность; место запуска уточняется. `OPENAI_API_KEY` в контейнере не настроен. Значения токенов не читались и не выводились. A2–A8 не закрыты: полная версия документов/предложений, свежесть и география источников, устойчивость общей очереди, эталонная и живая оценка поиска, сценарии полной себестоимости и окончательная переработка экранов требуют своих проверок.

## Выпуск A1

- Код Автобота: `411fe84608c6555c3d08af2eefe51f34b9a6d5ff`; код CRM: `2063b7a22058de0a919e3788180152ad325e7e1f`. Оба коммита отправлены в `origin/main` и опубликованы через fast-forward. Локальный checkpoint Автобота сохранён без изменения.
- Закрытая копия: `/opt/crm-backups/autobot-refactor-20260914T174638Z`. Проверены 4 SQLite CRM и 11 SQLite Автобота, включая сохранённые ранее копии; восстановление проверено в отдельных файлах. 4 исходных нарушения внешних ключей CRM сохранены без изменения, у Автобота — 0. Сохранены файлы данных, `.env`, исходные архивы и manifest.
- Образы отката закреплены до сборки: `crm-crm:rollback-4a2b14968a36`, `crm-autobot:rollback-3f548de73824`. Образы выпуска: `crm-crm:release-2063b7a22058`, `crm-autobot:release-411fe84608c6`. Сборка выполнена из архивов точных commit в `/opt/crm-releases/<sha>/source`.
- Оба новых образа прошли проверки без сети и рабочих данных: граница принятия HTML в CRM; кандидаты, единицы и неполное покрытие в Автоботе; импорт Flask и `/healthz`. После активации проверены HTTPS, карточка тендера, список смет, точное содержимое CSS, версии контейнеров и SQLite. Приватные `/api/users` и `/api/projects/1/market-analysis` без входа возвращают 401.
- В опубликованной карточке `0171200001926000664` текущие серверные данные дают **5 из 127** позиций с ценой, **4,4% суммы**, **16 033 003,08 ₽** без подтверждённой цены. Это другой срез рынка, чем сохранённая локальная копия; суммы сметы совпадают. Видны 13 исходных файлов. Мобильная ширина 390 px без горизонтального переполнения; ошибок console в проверенном сценарии нет.
- Прямые маршруты Автобота `/tenders` и `/estimates` сохраняют прежнюю публичную доступность. Проверка и разграничение прямого доступа входят в A8; отказ приватных API CRM не доказывает защищённость всех данных Автобота.

Следующий шаг: A2/A5 — свежесть, география и смысловое соответствие предложений, включая различие ручного и механизированного способа работ. Затем восстановление очереди и остальные критерии плана. Доступность внешнего исполнителя уточняется; встроенный поиск на сервере сохраняется.
# Пакет доказательств A2/A5 — 15 сентября 2026

Опубликован AutoBot `ff412bfcd68b91ea51718f19ff144b10e8f4675e` (20 файлов). В расчёт принимаются свежие предложения с подтверждённой единицей и областью поиска. Каждая поисковая фраза содержит регион; изменение региона требует повторного поиска. Сохранённая страница сохраняет дату получения, неизвестная дата не заменяется сегодняшней. Индекс повторно проверяет дату и характеристики, хранит регион и доказательство в существующих аудиторских JSON; SQL-схема и рабочие записи не мигрировали.

Отсекаются явные расхождения способа работ, марки бетона и фракции щебня, статьи, диапазоны и цены «от». Цена за час больше не подхватывает случайное упоминание м³ в соседнем тексте. Склонения названий мест учитываются консервативно: Ярославский район Москвы не является Ярославлем. В медиану входит одно предложение от известного продавца/домена; остальные ссылки остаются доступны. Базовая цена сохраняет дробную точность до денежного расчёта строки. Карточка показывает дату проверки и строку о местной работе/доставке; необоснованный процент «доверия» убран.

Проверки: **291 Python-тест + 20 subtests**, все прошли в изолированной копии с отключённой сетью. Новые случаи проверяют срок годности, неизвестную дату, изменение региона, условные цены, спецификации, единицы и независимые источники. Старые тесты извлечения цены «от» сохраняют проверку правильной суммы/единицы, но теперь ожидают кандидата. CRM-код в этом пакете не менялся; полный CRM-прогон 456 Python + 49 frontend-наборов относится к A1 и повторно не запускался.

Живые проверки: три ограниченных запуска по щебню, бетону М300 и земляным работам в Ярославле, только бесплатный web, без ключей и Авито. Первый выявил чужой регион и час вместо м³; второй — совпадение Ярославского района с Ярославлем и статью с диапазоном. В заключительном запуске (`tmp/autobot-refactor-20260914/live-probe-20260915T025644Z`) за 10,86 / 18,45 / 7,61 с получены 3 / 3 / 0 кандидатов и **0 точных подтверждённых предложений**. Ошибки поставщиков/поисковиков сохранены. Это подтверждает защиту от ложного результата, но **не подтверждает достаточную полноту поиска**. A5/A8 целиком не закрыты. Платные источники и внешний исполнитель ещё не настроены; вопрос о месте работы исполнителя остаётся открытым.

UI: на копии 127 позиций старые цены без регионального доказательства честно дают 0% покрытия. Дата и причина видны в панели, ссылка на оригинал сохранена. В текущей сессии фактически проверено окно 768×1024; инструмент изменения размеров не применил запрошенные 390/1280 к существующему окну, новое окно открылось 1280×720. Все три ширины A1 проверены ранее; полный повтор A2 при 390/768/1280 не заявляется. В этом пакете CSS не менялся. Положительное состояние с новым региональным доказательством проверено в Python, отдельно в браузере не проверено.

Выпуск: backup `/opt/crm-backups/autobot-evidence-20260915T030424Z`, 11 SQLite, integrity ok, 0 нарушений FK, восстановление проверено. Откатный образ `crm-autobot:rollback-411fe84608c6`. Новый образ `crm-autobot:release-ff412bfcd68b`, SHA `eeeda72396f38a61368640c6e2b226ee99dbbc6702411d381dd76470596ca254`. До переключения активных агентских заданий не было. Изолированная проверка образа прошла без сети/рабочих данных; после выпуска HTTP 200 для главной, тендера и смет, health ok, CSS совпадает, закрытые API CRM возвращают 401, версия checkout и образа совпадает, SQLite/FK без изменений. Предыдущий checkpoint локального Автобота сохранён (SHA256 `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8`).

Следующий пакет A3: право завершить конкретную попытку задания, сохранение принятого результата до публикации, повтор после сбоя без дублирования и защита отмены от позднего ответа. Затем расширение доступных источников и исправление распознавания смет; одной заменой модели эти пробелы не устранить.

## Решение для A3, до реализации

Разделить проверку внешних страниц и публикацию результата. Проверка формирует сохраняемый пакет с отпечатком исходной сметы/позиции и регионом; публикация повторно сверяет исходные данные и не использует сеть. Принятый пакет хранится в существующем `result_json` очереди со статусом `applying`; DDL и преобразование прежних записей не нужны. Совместимый публичный статус остаётся `leased` с `delivery_pending=true`, пока пакет не опубликован. Короткая транзакция очереди сериализует отмену/публикацию, файловая блокировка сериализует чтение-слияние-запись Excel между процессами. После сбоя пакет повторяется идемпотентно. Каждая выдача задания получает токен попытки; старый клиент без токена допустим только при первой выдаче, при повторной выдаче токен обязателен. Приёмка: истёкшая попытка/поздний ответ/отмена не меняют отчёт; повтор завершения не повторяет публикацию; новый процесс восстанавливает принятый пакет до и после сбоя записи.

## A3 — опубликовано 15 сентября

Автобот `6fb06bcf65e7329adef0ff4dbd23652bfe0ba394`. Принятые результаты сохраняются в очереди до записи Excel и восстанавливаются при запуске Gunicorn или отдельного web UI. Истёкшая попытка, поздний ответ старого исполнителя, отмена и изменение исходной сметы не позволяют опубликовать устаревший результат. Повтор одинакового завершения идемпотентен; другой результат завершённого задания отклоняется. Служебный токен попытки и подготовленный пакет не выдаются в обычном списке/прогрессе. Контракт исполнителя описан в `auto_bot/docs/AGENT_MARKET_API.md`.

Файловая блокировка охватывает чтение, слияние и атомарную замену отчёта между процессами. Исправлена потеря результата соседней позиции при завершении обычного поиска. Старое наблюдение больше не перезаписывает свежую цену в индексе. При повторной публикации ранее принятого пакета свежие предложения сохраняются. Повторная проверка исходной позиции и региона выполняется перед публикацией без сети.

Проверки: **304 Python-теста + 20 subtests**, все прошли в изолированной копии без исходящей сети. Проверены истечение права на результат во время проверки страницы, повторная выдача тому же исполнителю, отмена после приёма, временная ошибка записи и HTTP 202, ошибка изменённого входа, восстановление новым процессом. В отдельном дочернем процессе воспроизведён аварийный `os._exit(23)` после атомарной записи Excel до фиксации статуса очереди; следующий процесс завершил задание с одной строкой и одним предложением. Также проверено освобождение файловой блокировки при смерти процесса. CRM-код и CSS не менялись; полный CRM-прогон и визуальные размеры повторно не запускались.

Выпуск: backup `/opt/crm-backups/autobot-delivery-20260915T033402Z`, 11 SQLite с успешной проверкой восстановления, integrity ok, 0 нарушений FK. Откатный образ `crm-autobot:rollback-ff412bfcd68b`; новый `crm-autobot:release-6fb06bcf65e7`, SHA `edda7d92c6f7f462cc21579dc05b5e2f9a96de4dbea126ba409f3f27eb9d6538`. Активных заданий перед переключением не было. Изолированная проверка образа охватила правила цен, очередь, идемпотентность, приватность токена и запуск восстановления. После переключения HTTP/health/CSS, версия checkout/образа и все SQLite прошли проверку. Для отката при появлении `applying` сначала завершить эти публикации новой версией; не восстанавливать старую БД поверх последующей работы. Исходный локальный checkpoint сохранён.

Не закрыто: повторяемое исполнение всех стадий скачивания/разбора, достаточная полнота живого поиска, работа внешнего браузерного исполнителя, полная экономика, оставшийся интерфейс и отдельная защита прямых страниц Автобота. Следующий наблюдаемый результат: поиск проверяет ограниченный пул кандидатов после неудачных первых результатов и находит пригодный источник, не ослабляя проверку доказательств.

## Решение следующего пакета поиска

Общий поиск одной позиции используется и в полном тендере, и в ручном поиске. Разделить количество выдаваемых цен и пул кандидатов: по умолчанию до 12 разных страниц и до 3 вариантов запроса на позицию, цель — до 3 независимых источников. Одинаковую ссылку не открывать повторно в вариантах запроса. Ограничить начало новых сетевых операций сроком; оставшийся срок уменьшает HTTP-таймаут. Частичный результат сохраняется с причиной остановки. Кешировать глубину полученной выдачи и отличать короткий кеш пустого результата от недельного кеша кандидатов; при расширении глубины обновлять старую короткую выдачу. Приёмка: пригодная четвёртая ссылка после трёх плохих находится; дубли не расходуют бюджет; при исчерпании времени/страниц новые запросы не начинаются; полный тендер сохраняет те же проверки единиц, региона, количества и цены, что одиночный поиск.

## Расширенный поиск A5 — опубликовано 15 сентября

Основной пакет `010b0bbf3aa483e6152fcafde613e7aa93186b4a`, затем уточнение условных цен `3a4a61278a66d4036b04fb9357dbe1b3d4c9b010` — текущая опубликованная версия Автобота. Реализован общий ограниченный цикл, глубина кеша, короткий кеш пустого результата, независимые источники и естественный вариант запроса. Отдельные живые проверки DDGS показали результаты Яндекса и Startpage при пустом ответе Brave/Google; изменён порядок доступных движков, новые библиотеки и ключи не добавлялись. На странице точная строка получает приоритет перед условным рекламным текстом. Тариф за м³ и километраж не становится ценой материала. Подробности — `auto_bot/docs/MARKET_SEARCH.md`.

Проверки: основной пакет **316 тестов + 20 subtests**, после уточнения «от 3000 р» без точки — **319 тестов + 20 subtests**, все прошли без сети. Проверены четвёртый подходящий источник, дубли ссылок, разные продавцы, лимиты времени/страниц/запросов, уменьшение HTTP-таймаута, расширение кеша, неизвестная дата кеша и выбор точной строки. Самопроверка diff выполнена, независимого ревью не было. CRM-код и CSS не менялись; их проверки повторно не запускались.

Живой запуск `live-probe-20260915T040811Z`: щебень — 3 кандидата, 0 подтверждённых (28,06 с); бетон М300 — 3 предложения, 2 подтверждённых (31,14 с); земляные работы — 3 кандидата, 0 подтверждённых (30,58 с). Найдены точные цены на страницах СМИТ и БетонТоп в Ярославле; это предложения сайта, не полученные коммерческие предложения. Покрытие всей сметы и полная стоимость исполнения не приняты.

Отдельный запуск точного release-образа на сервере прошёл в контейнере без рабочих данных, ключей и Авито; DDGS 9.16.0 (локально 9.13.0). За 88,88 с он нашёл те же две страницы и ложное третье подтверждение «от 3000 р». Обнаруженная форма рубля исправлена в общем контракте чтения/поиска; повтор реального сохранённого пакета через `confirmed_prices` оставил только две точные цены. Новый сетевой поиск после изменения этой регулярной проверки не повторялся; сохранённый пакет и образ проверены. Логи: `tmp/autobot-refactor-20260914/server-search-probe.log`, `replay_server_evidence.py`; серверный лог в каталоге release `010b0bb…`. Цены проверки не записывались в рабочие отчёты.

Выпуски: backup `/opt/crm-backups/autobot-search-20260915T041302Z`, rollback `crm-autobot:rollback-6fb06bcf65e7`; затем backup `/opt/crm-backups/autobot-price-terms-20260915T041815Z`, rollback `crm-autobot:rollback-010b0bbf3aa4`. Каждый содержит 11 проверенных SQLite, integrity ok, 0 нарушений FK; восстановление проверено. Текущий образ `crm-autobot:release-3a4a61278a66`, SHA `f808d4b54b349e73111764f7c9a387b032d90781be35f4f35cc9496e3ed84d70`. Проверка образа без сети, HTTP/health/CSS, совпадение head/image и сохранность SQLite прошли. Локальный checkpoint сохранён.

## Решение для защиты прямого доступа A8

На сервере Nginx направляет `/autobot/`, `/estimates`, `/tenders`, `/merge-report`, `/research` и группу API напрямую в Flask. Вход в оболочку CRM не защищает эти пути. Добавить проверку доступа через существующую сессию и `user_can_access_autobot`: главный администратор, администратор, директор и прораб сохраняют доступ; анонимный пользователь и прочие роли не получают сметы, документы и действия. Nginx выполняет внутренний запрос к CRM до обращения к Автоботу; отказ/ошибка проверки закрывает запрос. Служебные worker-URL сохраняют отдельную Bearer-проверку пяти обработчиков Flask и прежние параметры proxy; непрефиксный proxy CRM не меняется, статика и внутренний health сохраняются. Проверка также отклоняет обращение браузера с постороннего сайта. Приёмка: матрица ролей CRM, реальный Nginx на изолированных тестовых upstream, отсутствие обхода через `/autobot/`, сохранение тела POST и отдельного worker API, публикация с резервной копией Nginx и проверкой `nginx -t` до reload. Рабочие пользователи и сессии для испытаний не создаются.


## Прямой доступ A8 — опубликовано 15 сентября

CRM `9eb63133af000de85350636b88315bb0b7dc5104`, образ `crm-crm:release-9eb63133af00`, SHA `55a62dcb863f6d234e51ef72b7db3093a0537202c4bff03e314151a692ef7dbd`. Nginx проверяет сессию и прежние роли CRM перед шестью группами прямых маршрутов Автобота. Проверка Origin выполняется с каноническим публичным адресом CRM; схема запроса также передаётся. Служебный префикс исполнителя сохранён с его Bearer-аутентификацией, прежним лимитом загрузки и HTTP-параметрами. Статика и внутренний health доступны. Правила и установщик находятся в `deploy/nginx`.

Проверки до выпуска: целевой `test_autobot*.py` — **38 тестов**, затем **464 Python-проверки и все 49 frontend-наборов**, без падений (`tmp/checks/20260915-093149-ade13b`). В новой тестовой фикстуре сначала не был задан канонический адрес CRM; исправлена конфигурация фикстуры, проверка чужого Origin сохранена. Настоящий Nginx на отдельном loopback-порту с синтетическими upstream проверил анонимный/запрещённый/разрешённый доступ, alias, POST с Origin и сохранением тела, пустое тело auth-subrequest, закрытый ответ при сбое CRM, внутренний маршрут, Bearer и границу namespace исполнителя. Лог `tmp/autobot-refactor-20260914/nginx-access-probe.log`. Дифф рабочей конфигурации до установки просмотрен; остальные proxy-директивы сохранены. Самопроверка не является независимым ревью.

Backup `/opt/crm-backups/autobot-access-20260915T043818Z`: CRM, 4 SQLite с проверенным восстановлением, 4 прежних нарушения FK сохранены, конфигурация Nginx. Откатный образ `crm-crm:rollback-e53174f9cd8b`. После запуска нового контейнера первый запрос готовности получил ConnectionReset; продолжена проверка уже установленной версии, затем отдельно выполнена установка Nginx. Повторного переключения версии или изменения бизнес-данных не было. `nginx -t` прошёл перед reload и после выпуска. Исходный SHA Nginx `0afe9869c7df77b921ea655acc0033a25bee530482d81a7262752ddd83f4cb63`, опубликованный `56b5422f0eeb465be0a348108d085f7922eb949bdaa48915896d001eee2109b8`.

После выпуска все проверенные прямые страницы, отчёты, пользовательский API и POST запуска без сессии вернули 401; приватное содержимое не выдано. Публичная CRM — 200; CSS совпадает; внутренние health и карточка тендера работают. Оба публичных адреса статуса исполнителя без токена — 401, с существующим токеном — **200** и `durable_completion`; значение токена не выводилось, claim/запись заданий не выполнялись. Контейнер и время старта Автобота остались прежними. Git/образ CRM совпадают, SQLite integrity/FK прошли. Вход сотрудника через браузер production отдельно не проверялся; разрешённые роли проверены в изоляции. Рабочие пользователи и сессии для проверки не создавались.

Следующий пакет: встроенный исполнитель только web-заданий из существующей устойчивой очереди. Главный поиск по тендеру уже запускается на сервере; поиск выделенных позиций пока может ожидать внешний браузер. Встроенный исполнитель должен соблюдать те же бюджет, проверку исходной позиции, право на попытку и отмену. Авито остаётся за отдельным браузерным исполнителем; его местонахождение ещё не подтверждено.


## Решение для встроенного web-исполнителя

Использовать существующую SQLite-очередь, lease, durable acceptance и публикацию, без нового брокера. Один серверный исполнитель на все процессы Gunicorn выбирает только web-задания; Avito сохраняет отдельный браузерный путь. Смета и регион фиксируются до поиска, попытка продлевается heartbeat, отменённая/истёкшая попытка не публикуется. Число запросов, страниц и время ограничиваются общим поиском. Сохранение общего и выбранного поиска объединяет наблюдения по дате для одной страницы; новый кандидат заменяет прежнее подтверждение, а пустой поздний поиск не стирает свежую цену. Проверка: изолированная очередь с настоящим отчётом, отмена/истечение во время поиска, восстановление после ошибки публикации, разделение web/Avito, исключение одновременных лидеров и повтор полного AutoBot-набора. Внешние сообщения и рабочие тестовые задания не создаются.


## Web-исполнитель — проверен перед выпуском

В изолированной копии реализованы выбор только web-задач, один лидер между процессами, heartbeat/отмена, сохранение результата через устойчивую доставку, общий ограниченный поиск и проверка исходной сметы/региона. Новые web-задания имеют максимум 2 попытки; пустой результат не запускает повтор. Поиск не требует внешнего браузера, Avito и ключа LLM. Внешний API получил необязательный фильтр mode, старый вызов без фильтра сохранён.

Сохранение общего и выбранного поиска объединено по свежести наблюдения. Пустой результат и неоткрытая страница не стирают свежую точную цену. Новая открытая страница с непригодной ценой переводит прежнюю запись индекса в candidate; поздний старый результат не восстанавливает её. Аудит сохраняется, таблицы/колонки не меняются. Общий поиск также проверяет изменение сметы/региона перед записью. Найденное при просмотре diff изменение объектов эквивалентных строк изолировано копированием предложения перед повторной проверкой.

Полный набор AutoBot: **346 тестов и 20 subtests прошли**. Добавлено 27 проверок исполнения, ограниченных повторов, отмены/истечения, смены сметы и региона, восстановления без сети, разделения очередей, единственного лидера, API и сохранения/отзыва цен. На первом полном прогоне падало прежнее ожидание max_attempts=1; актуальный лимит 2 закреплён отдельным поведением повторов. Старые проверки восстановления после гибели процесса и публикации Excel также прошли. Самопроверка diff не является независимым ревью.

Браузерная проверка новых подписей не выполнена: создание вкладки localhost завершилось ERR_BLOCKED_BY_CLIENT; документированный troubleshooting не дал доступного исправления. HTTP-запрос к локальному preview через текущую среду также вернул 502. Раскладка и CSS этого пакета не менялись; прежняя проверка A1 на 390/768/1280 сохраняется как историческая, новые подписи на этих размерах не засчитываются. API проверен Flask-клиентом в изоляции. Перед публикацией проверить образ, роли через публичный proxy, старт потока и отсутствие активных рабочих заданий.


## Web-исполнитель — опубликовано 15 сентября

Автобот `62354edd36d84b4c5a69c01a0166489a0777ce51`, образ `crm-autobot:release-62354edd36d8`, SHA `3636b82586daced6e7f62c7ba1cc130140d788670b4e9304818f44deb7530c98`. Backup `/opt/crm-backups/autobot-web-worker-20260915T050802Z`: 11 SQLite, integrity ok, 0 нарушений FK, восстановление проверено, исходники/конфигурация/данные сохранены. Rollback `crm-autobot:rollback-3a4a61278a66`. Локальный пользовательский checkpoint не изменён.

В собранном образе без сети и рабочих томов проверены старт двух служебных потоков, web-only поиск, приём результата и запись настоящего временного Excel с 2500 руб/м³. До переключения queue, parsing и общий рынок были без активных заданий. После выпуска Git/образ совпали, health работает, API возвращает executor=server. На host проверено, что блокировку лидера действительно держит процесс Автобота; одного наличия файла для этой проверки недостаточно. Очередь осталась пустой, рабочие задания для испытаний не создавались. CSS совпадает. Прямые страницы и CRM API без сессии по-прежнему возвращают 401, публичная оболочка — 200. Все 11 SQLite прошли integrity/FK после запуска. Логи `tmp/autobot-refactor-20260914/web-worker-*.log`.

Следующий пакет A4: отделить свежий поиск ЕИС от возобновления checkpoint, проверить получение/версии документов и происхождение строк сметы. Отдельно остаются полная приёмочная выборка поиска, расположение внешнего браузера Авито, сценарии экономики и оставшийся интерфейс.


## Решение A4: свежий поиск и возобновление документов

Обычный запуск и кнопка «Найти новые закупки» всегда выполняют новый поиск. Продолжение скачивания — явный режим, использующий только незавершённый checkpoint не старше 24 часов с теми же фильтрами. Каталожный поиск не стирает сохранённую очередь документов. Перед заменой/закрытием checkpoint сохраняется его копия; запись атомарна. Сетевой провал скачивания не считается успешно завершённым тендером. Один поиск держит межпроцессную блокировку; точечная обработка отдельного тендера сохраняет прежний контракт.

Воронка различает карточки, уникальные закупки, причины отсева, ограничение выдачи, уже известные/новые и результат доступа к ЕИС. Неизвестная цена/дата не выдаётся за соответствующую фильтрам. Бизнес-дата вычисляется при операции в том же PMBI_TZ_OFFSET_HOURS, что CRM. Временной бюджет ограничивает начало следующих запросов. Результат и возможность продолжения доступны после перезапуска страницы через API; интерфейс показывает причину пустого результата и отдельное продолжение документов. Профили с редактированием регионов/слов и безопасная распаковка остаются отдельными частями A4 после этого пакета.

Проверка: изолированные поиск/каталог без Telegram и сети, свежий поиск при старом checkpoint, явное продолжение с пропуском обработанных ID, истечение/повреждение/чужие фильтры, сохранение незавершённых загрузок при каталожном поиске, причины отсева, пустая/заблокированная страница, атомарность/два процесса и существующие тесты CLI/API. Рабочий локальный checkpoint не используется для тестов и не меняется.


## A4 — поиск ЕИС проверен перед выпуском

Реализованы свежий поиск по умолчанию, явное продолжение не старше 24 часов, история/атомарная запись checkpoint, блокировка параллельного общего поиска, сохранение незавершённых загрузок и отдельная воронка/причины отсева. Каталожный поиск сохраняет старую очередь документов и больше не запускает обход всех исторических этапов. При неизвестной цене/дате фильтр закрывается с объяснением. Бизнес-дата вычисляется при операции, нижняя граница давности включает целый день. Новый статус различает нераспознанную выдачу, подтверждённую пустую выдачу, частичный результат, прерывание процесса и ожидание продолжения. Снимок условий сохраняется в истории, денежные пороги — в копейках.

Добавлены 39 проверок. Полный AutoBot-набор: **385 тестов и 20 subtests прошли**, 16.15 сек. Первый целевой прогон выявил отсутствующий импорт DATA_DIR в API; импорт исправлен, положительный и отрицательный API-сценарии прошли. Flask отрендерил каталог на изолированных данных без сети; встроенный JavaScript прошёл `node --check`. Diff просмотрен отдельно на потерю данных, неоднозначный CLI, истёкшее продолжение, повтор, параллельный процесс и сохранность фильтров. Самопроверка не является независимым ревью.

Визуальная проверка нового резюме и дополнительной кнопки на 390/768/1280 ещё не выполнена из-за ERR_BLOCKED_BY_CLIENT локального preview. CSS не изменён; прежняя проверка A1 не считается проверкой этих новых состояний. Рабочий checkpoint сохранён, массовый поиск/скачивание и сообщения Telegram на production не запускались. Следом необходимы проверка собранного образа, небольшой изолированный запрос ЕИС и публикация при свободных рабочих задачах.


Дополнение перед переключением: preflight обнаружил новый служебный `data/market_web_worker.lock`, созданный ранее опубликованным исполнителем. Проверено, что исходники на сервере не менялись; добавлены точные ignore-правила для новых служебных файлов и истории поиска. Пользовательский checkpoint остаётся отслеживаемым и не изменён. Получен backup `/opt/crm-backups/autobot-tender-search-20260915T053659Z` с 11 SQLite, integrity ok, 0 FK, проверенным восстановлением, rollback `crm-autobot:rollback-62354edd36d8`.

Один запрос ЕИС из изолированного образа `4e8ff09` не завершил навигацию за 35 секунд; итог корректно отмечен как unavailable: 1 запрос, 0 загруженных страниц, 1 ошибка доступа, 0 карточек. Рабочие тома/секреты не подключались, контейнер завершён. Лог `tmp/autobot-refactor-20260914/eis-source-probe.log`, серверный лог в `/opt/crm-releases/4e8ff0924f7e8be9cf7e2a4a4332451dd7edc5e0/eis-source-probe.log`. Доступность и полнота ЕИС этим не подтверждены. При последнем просмотре добавлено ограниченное ожидание поздно появляющихся карточек, чтобы новая остановка на нераспознанной странице не обрывала ещё загружаемую выдачу. Случай закреплён тестом; окончательный набор **386 тестов и 20 subtests** прошёл. Повторный запрос ЕИС после изменения ожидания карточек не выполнялся: первый запрос не дошёл до загрузки страницы.


## A4 — свежий поиск и продолжение опубликованы 15 сентября

Автобот `71878c44931ac93689b8cbb9678a50d88d265386`; код поиска `a3bfaf8`, служебные ignore `4e8ff09`, ожидание карточек `71878c4`. Текущий образ `crm-autobot:release-71878c44931a`, SHA `ec2ee0a6ef5cf44e081461daaba1148b3769a638da9b0e111bce1b402ba766e1`. Backup `/opt/crm-backups/autobot-tender-search-20260915T053659Z`, rollback `crm-autobot:rollback-62354edd36d8`. Перед переключением общая очередь, разбор и сравнение были свободны. На production не выполнялись тестовые поиски, скачивания и отправка сообщений; живой запрос ЕИС выполнялся отдельно без рабочих томов.

В окончательном образе без сети и данных прошли прежние проверки доказательств/единиц/полноты, устойчивой доставки и web-исполнителя, затем свежий поиск на синтетической карточке, явное продолжение, неизменность checkpoint при каталожном запуске, рендер каталога и статус после сохранения. После переключения Git/образ совпадают, health — ok, web-исполнитель держит блокировку, новых активных заданий нет. API содержит search_summary/search_resume; обе новые области каталога присутствуют. 11 SQLite: integrity ok, FK без изменений. CSS совпадает; публичная оболочка — 200, защищённые прямые страницы и /api/parse-status без сессии — 401. Логи `tmp/autobot-refactor-20260914/tender-search-*.log`.

Доступность ЕИС остаётся неподтверждённой: изолированная навигация на сервере получила timeout. Визуальные состояния новой кнопки/резюме на трёх ширинах ещё не приняты. Следующий пакет — ограничения распаковки и качество/происхождение строк сметы; затем редактируемые профили, экономика и оставшийся интерфейс. Внешний браузер Авито и полная приёмочная выборка поиска всё ещё не подтверждены.


## A4 — ограниченная распаковка и происхождение файлов опубликованы 15 сентября

Автобот `b518414a6c933f08d7ea0dc5eb64d14c9958b34b`. Образ `crm-autobot:release-b518414a6c93`, SHA `75abca800aea1af4ab3f97928e9e09f558c0cc24b528bd4b6dc9020be848145d`. Backup `/opt/crm-backups/autobot-archives-20260915T061213Z` содержит проверенные снимки 11 SQLite и данные/исходники; rollback `crm-autobot:rollback-71878c44931a`. Пользовательский checkpoint с SHA256 `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8` сохранён. Миграции БД не выполнялись.

Распаковка вынесена в дочерний процесс с общими лимитами количества, размера, сжатия, времени и глубины вложенности. ZIP/RAR читаются потоком, опасные имена и ссылки отклоняются. Родитель останавливает зависший процесс вместе с внешним читателем RAR; Linux дополнительно ограничивает память до 256 МиБ, CPU и размер файла. Для Windows учтены длинные пути и стандартная установка 7-Zip вне PATH. Ошибка вложенного архива не публикует неполный результат исходного архива. Новый разбор с ошибкой архива сохраняет прежний отчёт; общий поиск продолжает другие тендеры, неуспешные сохраняет для продолжения. Карточка показывает причину и незавершённость.

Вход нового разбора — только текущие исходные архивы; старый extracted не добавляется в сметы и не сбрасывает глубину вложенности. Журнал содержит цепочку архивов, SHA256 и размер каждого файла. Завершённый кеш проверяется при повторе; изменённый производный результат сохраняется в карантине и восстанавливается из исходника. Каталоги версий остаются совместимыми с существующей корзиной исходных документов. Контроль PDF считает полные источники отдельно: одинаковые имена не объединяют распознанные файлы и официальные итоги.

Окончательный полный AutoBot-набор: **424 теста и 20 subtests прошли; 1 тест пропущен** (создание ссылки запрещено Windows), 22.34 сек. Linux-сценарий со ссылкой отдельно прошёл в собранном образе. Первый прогон выявил длинный путь Windows и недоступный импорт модуля в дочернем локальном Python — исправлены до коммита. При проверке настоящих RAR обнаружена установка 7-Zip вне PATH; добавлен поиск стандартного расположения, после чего обе копии распакованы. Самопроверка diff выполнена; независимого ревью не было.

На копиях настоящих документов проверены RAR `0121200004726000378/doc_8.rar` (6 Excel) и `0869200000226002022/doc_2.rar` (12 Excel), затем те же два архива в отдельном Linux-контейнере без сети и рабочих томов: **18 Excel извлечены**. На сервере этих исходных файлов не было: проверенные локальные копии переданы в собственную временную папку выпуска, после проверки удалены. Локальный ZIP `32615872880/doc_4.zip` (49 429 960 байт) отклонён как повреждённый; источник не изменён. Это проверка распаковки, не приёмка распознавания всех строк смет.

Перед переключением очередь, поиск и сравнение были свободны. В собранном образе прошли прежние проверки доказательств/единиц/покрытия, устойчивой доставки, web-исполнителя, поиска/продолжения, а также вложенный архив, повтор кеша, обход пути, Linux-ссылка и уборка временных файлов. После переключения Git/образ совпадают, health ok, web-исполнитель держит блокировку, очередь без новых активных задач. Публичная оболочка 200; защищённые прямые страницы/API без входа 401. 11 SQLite: integrity ok, FK без изменений. Логи `tmp/autobot-refactor-20260914/archives-*.log`, `archive-image-samples.log`, `archive-samples.json`.

CSS/раскладка не менялись. Новое текстовое предупреждение проверено по модели карточки; браузерная проверка его состояния на 390/768/1280 не выполнена (предыдущий локальный preview заблокирован клиентом). Предпросмотр архивов ещё использует прежний отдельный механизм. 7z, защищённые паролем и многотомные архивы требуют ручного открытия; отказ теперь виден. Качество распознавания Excel/PDF, коэффициенты и полнота смет остаются отдельными задачами A4. Следом — явные условия и сверка экономики A6, затем остальные профили/интерфейс/приёмочная выборка.

Пользователь подтвердил ввод доставки, накладных, резерва и налогов отдельно для каждого тендера. Произвольные проценты не использовать; незаполненные поля должны оставаться неизвестными. Выбранные значения не создают утверждённую экономическую базу объекта CRM.


## A4/A6 — сопоставление исходных сумм и адаптивность опубликованы 15 сентября

AutoBot `f3e70ca3994d9769d461d9c366a5bb5a64ef0539`, образ `crm-autobot:release-f3e70ca3994d`, SHA `d13617077735e22104248e8f9299f42a805342c555cbbb47c6d664bc72c7937a`. Backup `/opt/crm-backups/autobot-controls-20260915T064147Z`, откат `crm-autobot:rollback-b518414a6c93`. Миграций данных нет; пользовательский checkpoint сохранён.

Удалено автоматическое применение НДС 22% и признание полноты сметы по близости к НМЦК. Карточка и HTML показывают исходные суммы и справочную разницу; режим НДС/коэффициенты и полнота требуют подтверждения. Нулевое число распознанных PDF больше не заменяется числом файлов прежнего отчёта. Ошибки архивов доступны на обеих вкладках даже до появления первой сметы. Значения ячеек и имена источников экранируются в HTML-отчёте. Каталог адаптирует блок следующего шага по доступной ширине вместо жёстких минимальных колонок.

Полный изолированный AutoBot-набор: **428 тестов и 20 subtests прошли; 1 тест пропущен** (создание ссылки Windows), 20.81 сек. Linux-проверка ссылки прошла в собранном образе вместе с прежними проверками доказательств, единиц, покрытия, доставки, web-исполнителя, нового поиска/продолжения и распаковки. Дополнительно проверено отсутствие подбора НДС в образе. Самопроверка diff выполнена; независимого ревью не было. Полные наборы CRM не повторялись для изменения только AutoBot.

После перезапуска собственного изолированного preview браузер стал доступен. На **390, 768 и 1280 px** проверены каталог с возобновляемым поиском и длинной сводкой, карточки/кнопки, окно сопоставления, предупреждение архива с длинным именем и переход к исходным файлам. Найденное горизонтальное переполнение на 768 px исправлено; фактические ширины документа после исправления 375/753/1265 px. Проверены Escape и возврат фокуса из окна сверки. Сетевые задания и записи в production через браузер не запускались; временный checkpoint preview восстановлен. Это также закрывает прежнее ограничение визуальной проверки предупреждения архива и кнопки продолжения.

До переключения не было активных заданий. После выпуска Git/образ совпадают, health ok, исполнитель держит блокировку, очередь свободна; 11 SQLite integrity ok, FK без изменений. Публичная оболочка 200, закрытые страницы/API без входа 401; оба CSS совпадают по SHA256. Логи `tmp/autobot-refactor-20260914/controls-*.log`. Полнота смет, качество массового поиска и экономика участия всё ещё не приняты. Следующий пакет — отдельные условия участия по тендеру с явными суммами, неизвестными значениями, историей и серверными правами руководителя.


## A6 — личные условия участия и три сценария опубликованы 15 сентября

CRM `e4f64aa9e6c8dcc3acbdc5d1aba44e88be975614`, образ `crm-crm:release-e4f64aa9e6c8`, SHA `4262718ba6edaae246233666e3b825a018d67153a563b4b948e4b6c27fd59268`. AutoBot `dba93057576bfbb9b5960ca43bf1ce2ae5e17ad6`, образ `crm-autobot:release-dba93057576b`, SHA `f1c509f8fda201abaa3db124d0e7d519e6a206b173d87aed2091cc76a0d6373b`. Backup CRM `/opt/crm-backups/autobot-scenarios-crm-20260915T070935Z`, Bot `/opt/crm-backups/autobot-scenarios-bot-20260915T070938Z`; rollback соответственно `crm-crm:rollback-d7a57fc8f86d` (прежний код 9eb6313 с последующими документами) и `crm-autobot:rollback-f3e70ca3994d`.

В карточку добавлен редактор базового, осторожного и оптимистичного сценария. Пользователь задаёт цену предложения, материалы, работы, технику, доставку, накладные, комиссии/стоимость гарантий, финансирование, резерв и налоги. Все суммы в копейках без НДС; отдельные суммы НДС не входят в маржу. Процентов по умолчанию нет. Неизвестный расход не становится нулём. Подтверждаются полнота/отсутствие двойного счёта, налоговая база и основание. Часть найденного рынка справочная, автоматически в расходы не переносится. Условия не меняют экономику объекта CRM.

Сохранение и неизменяемая история принадлежат CRM; автор — из текущей сессии, дата — business_time на момент записи. Новый отдельный `data/tender_scenarios.sqlite3` создаётся при разрешённом обращении; существующие таблицы не мигрируют. Транзакция, версия и ключ операции защищают от параллельной замены и повторной доставки. AutoBot отдаёт только версию источников и справочные данные. При смене/недоступности источников прежние суммы сохраняются, итог не помечается готовым. Подробный контракт и откат — [docs/AUTOBOT_TENDER_SCENARIOS.md](docs/AUTOBOT_TENDER_SCENARIOS.md).

Полный изолированный CRM-прогон: **474 Python-теста + 49 frontend-наборов прошли** (`tmp/checks/20260915-115930-de1b91`, Python 206.86 сек). После уточнения пустой суммы затрат и двух дополнительных сценариев отдельно прошли **12 тестов экономики** (`tmp/checks/20260915-120306-f55611`). Полный CRM-набор после этой локальной правки не повторялся. Окончательная проверка синтаксиса нового JS прошла. Полный AutoBot: **431 тест + 20 subtests прошли; 1 пропущен Windows**, 21.02 сек. В собранном Linux-образе CRM все 12 новых тестов прошли снова; в Bot прошли прежние проверки очереди/поиска/архивов и новый контроль версии/копеек источника. Самопроверка diff выполнена, независимого ревью не было.

Браузер 390/768/1280 px: пустой и частичный сценарий, сохранение, точные суммы, перенос строк, кнопки и фокус доступны, документ не шире viewport (375/753/1265). На отдельной БД демонстрационный неполный сценарий не дал прибыли; после заполнения налогов остаток 110 000,01 руб. сохранился и открылся после перезапуска. Осторожный сценарий с другими материалами дал −89 999,99 руб., базовый сохранился. Изменение исходного файла сняло готовый итог; принятие текущего источника сбросило обе отметки подтверждения. У прораба нет финансовых полей/сохранённой прибыли в DOM; серверная матрица запрещённых ролей проверена тестами. Временный изменённый исходник восстановлен. Смена сценария сохраняет несохранённую форму в памяти; перезагрузка страницы её не сохраняет.

CRM и Bot опубликованы последовательно, Nginx не изменялся. Скрипт готовности CRM после пересоздания контейнера получил краткий ConnectionResetError; повторного развёртывания не делали: отдельная проверка подтвердила готовность, точную ревизию и доступ, затем опубликован Bot. Обе ревизии образов совпадают с checkout; текущая CRM получила текущий источник Bot по настроенному внутреннему URL. Статические файлы совпадают по байтам; новые API без входа 401. 4 прежние SQLite CRM и 11 Bot: integrity ok, FK без изменений (4 прежних CRM, 0 Bot); web-исполнитель держит блокировку, очередь без активных задач. Демонстрационные условия в production не записывались. Пользовательский checkpoint сохранён. Логи `tmp/autobot-refactor-20260914/scenarios-*.log`.

Этот пакет закрывает ввод, вычисление и историю условий A6, но не качество всей сметы/рынка, график платежей/кассовый разрыв, полную приёмочную выборку и остальные поисковые профили. Далее — ограничение оставшегося пути просмотра/разбора документов A4 и проверка качества на настоящих источниках.

## A4 — ограниченный предпросмотр опубликован 15 сентября

Bot `af110f3ef057a7aa92946660f60e7f49f5dbb9e9`, образ `crm-autobot:release-af110f3ef057`, SHA `5a61dc90d8d7045bcd78e50bdec5eab5f34b4b67c78be006712d9851656f66c9`. Backup `/opt/crm-backups/autobot-document-preview-20260915T073717Z`: 11 проверенных SQLite, данные и исходники; rollback `crm-autobot:rollback-dba93057576b`. CRM остаётся на коде `e4f64aa`; её документы обновляются отдельно. Nginx и рабочие таблицы не изменены, пользовательский checkpoint сохранён.

Excel/DOCX и чтение ZIP/RAR выполняются отдельным процессом, один обработчик на общую папку данных. Лимит 25 секунд; Linux ограничивает адресное пространство 768 МиБ, CPU и выходные файлы. На Windows проверены время/размер/уборка; ограничения памяти ОС там нет. Вложенные архивы используют общие лимиты и прежние проверки путей/ссылок. Excel явно отмечает усечение до 100 строк/30 колонок, текст читается до 2 МиБ и не портит UTF-8 на границе. После ограничения доступен оригинал. Исправлена запасная ссылка на скачивание неизвестного формата внутри архива. Это ограничение ресурсов, не полноценная песочница.

Полный AutoBot: **440 тестов + 20 subtests прошли, 1 пропущен** (Windows-ссылка), 26.20 сек. После уточнения UTF-8 и оформления отдельно **17 тестов прошли**, 9.01 сек; полный набор после этой правки не повторялся. Проверены настоящий дочерний таймаут и следующий запрос, параллельная блокировка, общий бюджет вложенности, глубина при переходе, unsafe path, HTTP 422 и неизменные байты оригинала. Самопроверка diff выполнена, независимого ревью не было.

Копии RAR `0121200004726000378/doc_8.rar` и `0869200000226002022/doc_2.rar` проверены локально и в новом Linux-образе без сети/рабочих томов: списки 6 и 12 Excel, первый файл каждого открыт (91 и 100 отображённых строк; второй явно усечён). Исходные SHA не изменились. Linux также прошёл Excel 100×30, DOCX, таймаут/повтор/скачивание элемента/блокировку/уборку и прежние проверки очереди, поиска, архивов и версии экономики.

Браузер на копии данных: ошибка лимита и широкая таблица при 390/768/1280 px, доступный оригинал и понятная причина. Найденное переполнение мобильного заголовка исправлено в CSS владельца; итоговые client/scroll widths 375/375, 753/753, 1265/1265. Таблица прокручивается внутри. Файлы проверки удалены по точному пути и SHA, viewport восстановлен. Нативный диалог скачивания не проверялся; HTTP-байты проверены тестом.

После выпуска: точная ревизия checkout/образа, health/API, статические байты, анонимный доступ 401, 11 SQLite integrity ok и 0 FK; web-исполнитель держит блокировку, активных задач нет. Работающая CRM получает версию от работающего Bot. Логи `tmp/autobot-refactor-20260914/preview-*.log`. Основной парсер всех строк Excel/PDF, профили ЕИС и полная приёмка качества рынка остаются открыты.

## A4 — загрузка текущего комплекта и повтор из карточки опубликованы 15 сентября

Bot `b82163781e59a129207a4ae886c737c6fbcdbf55` + совместимость `8ccce299eb7dd6cc16c8f3687d4f4fedf9d652e7`. Образ `crm-autobot:release-8ccce299eb7d`, SHA `d34b2082ee368a98ec4ff0e86b9aff385f56d87d6237aea7aa6e8bab21aad6d2`. Backup `/opt/crm-backups/autobot-document-downloads-20260915T081539Z`, rollback `crm-autobot:rollback-af110f3ef057`. Снимки 11 SQLite проверены с восстановлением. Рабочие таблицы/Nginx не менялись; пользовательский checkpoint с прежним SHA256 сохранён.

HTTP читается потоком в отдельном процессе: 128 МиБ/90 секунд на файл, Linux дополнительно ограничивает память/CPU/выход, на Windows лимита памяти ОС нет. Пустой, HTML, частичный ответ, неверный размер и повреждённая структура ZIP/Office отклоняются. Полный CRC архивов остаётся этапом распаковки. До активации должны успешно скачаться все ссылки; сетевой сбой одного файла сохраняет прежние документы и отчёт. Комплект ограничен 100 файлами/512 МиБ/600 сек скачивания; навигация по вариантам карточки имеет общий срок 180 сек.

Атомарный DOCUMENTS-журнал фиксирует текущий комплект с размерами и SHA. Новый/повторный разбор использует эти источники, старый лишний архив не включается. Разные документы с одним названием сохраняются раздельно, повтор одинаковых байтов не дублирует файл. Очистка дублей сохраняет имена текущего комплекта и не пересекается с загрузкой. Служебные incoming-файлы недоступны в списке и по прямой ссылке. Каталоги без нового журнала сохраняют прежний выбор; миграции нет. Старые версии источника экономики не меняются от одного обновления кода; реальное изменение комплекта/состояния меняет версию.

Карточка показывает причину на обеих вкладках и предлагает «Повторить скачивание». В «Ещё» есть обычное обновление документов. Новый POST использует проверенную HTTPS-ссылку ЕИС из каталога, резервирует запуск до старта потока, повтор получает 409. Статус с run_id восстанавливается при перезагрузке страницы; CSRF и внешний доступ через сессию CRM сохранены. В рабочей среде никаких проверочных загрузок не запускали.

Последний полный набор до небольших правок очистки/совместимости: **461 тест + 20 subtests прошли, 1 Windows-symlink пропущен**, 26.79 сек. Затем **28 тестов загрузки/очистки** и **27 тестов загрузки/источника экономики** прошли; полный набор после этих локальных изменений не повторялся. Проверены сохранение отчёта после неуспешной загрузки, следующий запрос, устаревший архив, изменённый SHA, одинаковые имена, блокировка очистки, серверное резервирование запуска, чужой Origin и неизменная версия legacy-контекста. Синтаксис JS проверен на отрисованном шаблоне. Самопроверка diff выполнена, независимого ревью не было.

Реальный HTTP на собственном loopback-сервере: полноценный файл, HTML, обрыв, зависание, повтор; байты предыдущего файла и уборка подтверждены. Такой же прогон прошёл в Linux-образе без внешней сети и рабочих томов, включая настоящий Excel, неполный комплект, повтор, очистку и API запуска. Все прежние проверки образа также прошли. При первой попытке прочитать локальную страницу для проверки JS системный proxy вернул 502; прямое соединение только с localhost прошло, код приложения не менялся из-за этого.

Браузер на копии данных: 390/768/1280, длинная причина, доступная кнопка, фокус статуса, ошибка/повтор, восстановление выполняющегося задания после reload. client/scroll: 375/375, 753/753, 1265/1265. Для клика использован локальный фиктивный исполнитель, реальный ЕИС не вызывался. Временный DOCUMENTS-файл удалён по точному пути/SHA, viewport восстановлен. После выпуска совпали checkout/образ, health, публичные статические файлы; новый анонимный POST вернул 401. 11 SQLite integrity ok, FK 0; очередь пуста, web-исполнитель держит блокировку, CRM читает актуальный Bot. Логи `tmp/autobot-refactor-20260914/downloads-*.log`.

Контракт и ограничения — [docs/DOCUMENT_DOWNLOADS.md](../code/auto_bot/docs/DOCUMENT_DOWNLOADS.md). Весь комплект не является одной файловой транзакцией: сбой активации сохраняет неуспешное состояние, разбор запрещён, заменённые версии доступны в корзине. Изменение файла во время OCR/Excel и ограничение самого парсера остаются следующим этапом. Доступность ЕИС/Авито и целевая точность рыночной выборки этим пакетом не доказаны.


## A4 — многотомный ZIP опубликован 15 сентября

Bot `56b16935c91bd137350fa31c40ac9229c0d10a3a`, образ `crm-autobot:release-56b16935c91b`, SHA `0e37d98050f908b25cb40486991f76fd357a71b9ec9769e6ff0136fa7fb7b998`. Backup `/opt/crm-backups/autobot-split-zip-20260915T084421Z`, rollback `crm-autobot:rollback-8ccce299eb7d`. Исходный CRM diff был пуст; пользовательский checkpoint Bot сохранён с SHA `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8`. БД и Nginx не менялись, проверены снимки 11 SQLite с восстановлением.

Уточнение прежнего аудита: `32615872880/doc_4.zip` является последним томом вместе с `doc_3.z01`. Старый результат «повреждённый ZIP» описывал отказ одиночного читателя, а не доказанное повреждение байтов. На изолированной локальной копии оба тома теперь дают полный CRC вложенного RAR размером 101 858 502 байта, SHA `e7dcc99cfe9dcb0ee8f4ba8a62124ba5de9fb17fe1a47821177165835e298c47`; затем успешно извлечены RAR и пять PDF, Excel нет. Исходные SHA обоих томов неизменны. Сначала длинный путь временной копии на Windows не позволил завершить распаковку; для превышения пути добавлена точная причина до создания файла. В короткой временной папке полный разбор прошёл за 1.89 сек.

Обычный ZIP32 с каталогом в последнем томе собирается во временную копию с пересчётом публичных смещений формата. CRC, перекрытия, пути и бюджет вложенности остаются в стандартном проверенном механизме. Общий вход всех томов ограничен 128 МиБ; старые оригиналы не переписываются. В происхождение и ключ кеша входят SHA/размер/имя каждого тома, проверка повторяется после чтения кеша. Изменённый первый том не подменяет прежний результат. Основной разбор передаёт текущий список документов, предпросмотр учитывает DOCUMENTS-журнал. Пропуск, лишний/неоднозначный том и `.z01` без завершающего ZIP останавливают новую смету. Старые `doc_N` допускаются только в единственном однозначном комплекте. ZIP64, каталог на нескольких томах и многотомный RAR пока не поддержаны. Загрузчик принимает самостоятельные `.z01 … .z99` без ложного требования полного ZIP-каталога; HTML и некорректное начало первого тома отклоняются. Найдены и включены ссылки `/223/purchase/public/download/download.html`.

Полный набор на промежуточной версии пакета: **479 passed, 1 skipped, 20 subtests**, 26.52 сек. После уточнения лимита предпросмотра/сообщения и тестов — **85 passed, 1 skipped**, 11.17 сек. После последней защиты от изменения тома во время проверки кеша — **54 passed, 1 skipped**, 6.23 сек. Полный набор после этих последних небольших изменений не повторялся. Пропущена прежняя проверка Windows-symlink; Linux-symlink прошёл при проверке образа. Выполнена самопроверка diff; независимого ревью нет.

Linux: реальный комплект отсутствовал на сервере, локальные публичные документы ЕИС переданы в отдельную папку выпуска и удалены по SHA после проверки. Контейнер без сети/рабочих данных, 512 МиБ/1 CPU: RAR и пять PDF извлечены, SHA совпали. Предпросмотр комплекта 97 МиБ вернул точную причину превышения общего лимита 32 МиБ до временной сборки. Все прежние проверки образа и новый трёхтомный ZIP, смещения позднего тома, кеш/CRC, пропущенный последний том и следующий запрос прошли.

Браузер на копии: полный маленький ZIP показывает два файла, отсутствие первого тома показывает точную причину с доступом к оригиналу. Проверены 390/768/1280 px, scroll/client совпадают, ссылки не выходят за экран. Первые снимки после смены viewport получались до перерисовки, повторные снимки после свежего состояния подтвердили раскладку. Тестовые файлы удалены по точным путям/SHA, viewport восстановлен. После выпуска совпали Git/образ, health, статика, внутренний запрос CRM; анонимный API остаётся 401, очередь пуста, web-исполнитель держит блокировку, SQLite integrity ok/FK 0. Логи `tmp/autobot-refactor-20260914/split-*.log` и `split-real-probe.json`.

Этот выпуск подтверждает чтение полного комплекта документов, а не качество всех строк OCR и рыночного поиска. Следующий пакет — конечный основной Excel/PDF-разбор, явная ошибка чтения вместо пустой сметы и проверка версии исходников перед публикацией отчёта. Условия экономики по выбору пользователя остаются отдельными для каждого тендера, без произвольных процентов.


## A4 — ограниченный основной разбор опубликован 15 сентября

Bot `1a07aac32270a9029405509f748942aac0d129f1`, образ `crm-autobot:release-1a07aac32270`, SHA `bcc2bc4add17c8af6304ec4f84bc22b7463b44777e99a41977e0a1aea7c5df63`. Backup `/opt/crm-backups/autobot-estimate-parser-20260915T093149Z`: 11 SQLite проверены с восстановлением, FK 0; rollback `crm-autobot:rollback-56b16935c91b`. CRM работает на прежнем коде `e4f64aa`; обновляются только документы. Nginx, рабочие данные и исходный пользовательский checkpoint не изменены.

Все три сценария CLI используют один обработчик: ограниченный дочерний Excel/PDF-парсер, затем подготовка отчётов и проверка SHA под общей блокировкой текущего комплекта. Лимит 300 секунд; 100 файлов, 128 МиБ на файл/512 МиБ всего, 100 листов, 50 000 строк на лист, 256 колонок/2 млн ячеек, 250 страниц PDF/25 млн пикселей страницы, 50 000 позиций и 16 МиБ результата. Linux ограничивает память 1 ГиБ, CPU и выходные файлы. На Windows проверены время/размер/завершение дочернего процесса; ограничения памяти ОС там нет. Документ не исполняется как программа, но это ограничение ресурсов, а не полная песочница. Дочерний процесс не восстанавливает ключи интеграций через dotenv.

Ошибка чтения отличается от прочитанного несметного файла. Ошибка одного выбранного источника, пустой итог, превышение лимита или смена SHA сохраняют прежний отчёт. Общий поиск продолжает другие тендеры и учитывает отказ. XLSX/HTML/контроль/завершённый PARSE_RUN готовятся в своей временной папке. Обычная ошибка замены откатывает уже заменённые файлы; при ошибке самого восстановления резервные копии остаются. Четыре файла не образуют одну транзакцию файловой системы: аварийное убийство процесса между заменами может оставить разные поколения. В таком случае незавершённый статус блокирует подтверждение, копии остаются в staging; автоматическое восстановление аварии ещё не реализовано.

Карточка показывает причину и повтор разбора на обеих вкладках, в том числе до первого отчёта. Существующий POST rebuild-report сохраняет HTTP 200, получает run_id и резервирует запуск до старта потока; дубли получают 409. Состояние восстанавливается после обновления страницы. Текстовый PDF-разбор теперь использует уже установленный PyMuPDF вместо отсутствовавшей зависимости pypdf; запасной разбор явно требует проверки строк. Версия экономики учитывает состояние и источники разбора, прежняя версия старых карточек без журнала сохранена.

Проверки: полный набор **502 passed, 1 skipped, 20 subtests**, 79.77 сек. После последнего уточнения сообщения CLI — **42 passed**, 38.05 сек; полный набор после этого не повторялся. Пропущена Windows-symlink, Linux-ссылка проверена в образе. Проверены настоящие дочерние процессы и Excel с суммами 4304.24/86564.48, таймаут и следующий успешный запуск, сохранение трёх прежних файлов при повреждении/подмене, частичная замена/откат и сохранение резервной копии при повторной ошибке, блокировка комплекта, первый неудачный и следующий успешный тендер общего запуска, CSRF и run_id.

Уточнение прежних записей о локальной изоляции: установленный embedded Python игнорирует PYTHONPATH. Ранее заданный через него sitecustomize не загружался; утверждать, что этот механизм блокировал сеть в прошлых локальных прогонах, нельзя. Теперь отдельный временный Python с явным ._pth загружает проверяемый socket guard, в том числе в дочерних процессах; полный набор выше повторён именно так. Исходная установка Python не изменена. Ранее использовались тестовые подмены/удалённые ключи, а Linux-проверки выполнялись с --network none.

Настоящий русский PDF проверен локально с существующей зависимостью PyMuPDF 1.28.2, установленной только в собственную временную папку: две строки 12500/45500, запасной текстовый разбор, повреждение с сохранением отчёта и отказ огромной страницы до растра. Linux-образ без сети/рабочих томов, 512 МиБ/1 CPU прошёл Excel, публикацию, повреждение, таймаут/повтор, настоящий PDF через путь установленного OCR и текстовый fallback, лимит растра и symlink; прежние проверки очереди, доказательств цен, архивов, загрузки и экономики также прошли. Это не подтверждает точность OCR на всех реальных ЛСР.

Браузер на копии данных: 390/768/1280 px, длинная ошибка без переполнения, доступная кнопка, блокировка на время запуска, восстановление работающего статуса после перезагрузки и повтор после отказа. Для UI использовался явно тестовый исполнитель без ЕИС/Telegram. client/scroll widths совпали: 375, 753, 1265. Синтаксис итогового встроенного JS проверен Node. Тестовый PARSE_RUN удалён по точному пути/SHA, viewport сброшен. Выполнена самопроверка diff; независимого ревью не было.

После публикации: Git/образ совпали, health/API/статические байты и запрос CRM к Bot проверены; анонимные страницы/API остаются 401, очередь без активных задач, web-исполнитель держит блокировку, 11 SQLite integrity ok/FK 0. Производственный разбор реальных тендеров для теста не запускался. Логи `tmp/autobot-refactor-20260914/parser-bot-*.log`, `parser-pdf-probe.log`; временные помощники не входят в поставку.

Далее — качество извлечения физических позиций и единиц PDF/Excel на сохранённых документах, затем редактируемые поисковые профили и компактный общий экран. Приёмочная выборка рынка, исполнитель Avito и график платежей A6 остаются открыты; весь рефакторинг ещё не завершён.


## A4 — целостность позиций PDF опубликована 15 сентября

Bot `65f45d649605b1ae35e54ac32ed05098a8413c0a`, образ `crm-autobot:release-65f45d649605`, SHA `e69b18be010acdfff3f3cf49eebcfa11cec74e59a871734ceb9b87736dcaf472`. Backup `/opt/crm-backups/autobot-pdf-quality-20260915T101551Z`: 11 SQLite проверены с восстановлением, FK 0; rollback `crm-autobot:rollback-1a07aac32270`. CRM работает на прежнем коде `e4f64aa`. Исходные документы, рабочие данные и пользовательский checkpoint не мигрировали.

Отдельные физические позиции PDF больше не объединяются по совпадению названия и кода. Координатный ID сохраняет принадлежность рыночной цены строке; точный повтор одного OCR-якоря удаляется. Обе ветки чтения PDF сохраняют исходные единицы и масштаб: «100 м2» с количеством 0,2 не превращается в другую базу без изменения цены. Неизвестное количество не становится единицей; единица не выводится из предположений о материале. Повторяющиеся строки текстового PDF также сохраняются.

Итог основной позиции берётся из её собственного «Всего/Итого по позиции», включая продолжение на следующей странице. Распознанные вложенные ресурсы привязываются к родителю и сохраняются в журнале, без двойного прибавления к итогу. Наклон строки OCR и наблюдаемый вариант «no» вместо «по» учтены; числовой столбец не склеивает коэффициенты из поясняющего текста. Пустая сумма остаётся отдельной строкой с неизвестным покрытием; если суммы неизвестны у всех строк, прежний отчёт сохраняется. Журнал указывает отсутствующие количество, единицу или сумму, исходные ресурсы и нераспознанные суммы.

Проверки финального кода: **519 passed, 1 skipped, 20 subtests**, 66.16 сек, изолированная копия и проверенный запрет внешней сети. Единственный Windows skip — ссылка файловой системы; Linux-ссылка проверена в образе. Дополнительно синтетический двухстраничный PDF прошёл настоящий OCR в финальном Linux-образе: четыре одинаковые физические позиции сохранены, исходная единица «100 м2», одна неизвестная величина, собственные ключи рынка, повтор с теми же ID и SHA, staging очищен.

Настоящая сохранённая шестистраничная смета визуально сверена по всем страницам, отрисованным Poppler. В финальном образе выполнен новый полный OCR и публикация на временных данных, без внешней сети и без подключения рабочих каталогов: **15 из 15 итогов основных позиций совпали с отдельно переписанным эталоном**, три распознанных ресурса сохранены у родителей. Четыре единицы и одно количество остались неизвестными; остальные проверенные единицы/количества совпали. Подробный эталон и финансовые значения остаются локальными QA-данными, в Git не включены. Это приёмка одной сметы, а не всех макетов OCR: названия/коды, полный ресурсный состав и каждый коэффициент ещё не подтверждены. Вторичный OCR местами дал неверные цифры, поэтому автоматической подстановки таких догадок нет.

Браузерная проверка на копии карточки при **390/768/1280 px**: три одинаковых названия остаются отдельными строками, две строки выбираются независимо, исходный масштаб единиц виден; строка без суммы остаётся с «—», сводка показывает неизвестную полноту вместо ложного процента. Ширина страницы совпала с шириной содержимого, переполнения нет. Выбор снят, viewport сброшен, временные файлы восстановлены по SHA. Реальный поиск цен из браузерного теста не запускался.

После публикации проверены Git/образ, health/API, защищённые анонимные запросы, совпадение статических файлов, запрос работающей CRM к AutoBot, блокировка web-исполнителя и отсутствие активных задач, 11 SQLite integrity ok/FK 0. Отдельная самопроверка diff выполнена; независимого ревью не было. Логи `tmp/autobot-refactor-20260914/quality-bot-*.log`, `quality-final-real-image.log`; временные помощники не входят в поставку.

Следующий пакет — сохраняемые профили ЕИС и ясный запуск поиска. Общая приёмочная выборка рынка, доступность ЕИС, исполнитель Avito, полный OCR, аварийное восстановление публикации, денежный график A6 и общий компактный интерфейс остаются открыты.


## A4/A7 — профили ЕИС и маршруты запуска опубликованы 15 сентября

Профили реализованы в Bot `0f98d71`, совместимость production-маршрутов — `97e6316c3dc89c33fc9b27d162e020667dadf06b`. Финальный образ `crm-autobot:release-97e6316c3dc8`, SHA `ed22494689d5998fa1c7962219ac22a047f7fb05d35c7153ded786a7d22de88b`. Последний backup `/opt/crm-backups/autobot-profile-routes-20260915T104859Z`, rollback `crm-autobot:rollback-0f98d71215ac`; более ранний backup профилей `/opt/crm-backups/autobot-search-profiles-20260915T103921Z`, rollback `crm-autobot:rollback-65f45d649605`. В обоих backup 11 SQLite проверены восстановлением, FK 0. Рабочие данные не мигрировали, исходный checkpoint сохранён; CRM работает на прежнем коде `e4f64aa`, Nginx не изменялся.

Кнопка «Найти тендеры» открывает сохраняемые условия: регионы, темы, границы суммы, давность и предел выдачи. До 20 именованных общих профилей, атомарный JSON до 128 КиБ и межпроцессная блокировка; версия списка запрещает перезапись чужой правки, повтор после потери ответа не добавляет версию. Повреждённый файл и ошибка записи сохраняют прежние данные. Профиль не создаётся на диске от одного чтения. Выбранный профиль запоминается в браузере, значения восстанавливаются с сервера.

Деньги в условиях — целые копейки, пустая граница не заменяется числом; неверный диапазон и лимиты отклоняются. Неизвестная цена закупки остаётся причиной отсева. Этап «Подача заявок» пока фиксирован и видим. Регион используется в текстовом запросе ЕИС; это не подтверждает место исполнения. На один профиль до 60 сочетаний регионов/тем, действующий общий срок поиска сохраняется. CLI без новых параметров сохраняет прежние значения. API/CLI принимают профиль либо полный снимок; дочернему процессу передаются уже проверенные условия. История и checkpoint хранят снимок, продолжение восстанавливает исходные условия даже после изменения профиля. Старые checkpoint и подписи остаются совместимыми.

Первичная production-проверка обнаружила 401 при загрузке нового JS: путь `/static/tender_search_profiles.js` не входил в проксируемые статические файлы. Просмотр текущего Nginx также показал, что прежний `/api/start-parse` направлялся в CRM. Исправлено без расширения публичного доступа: JS через `/tenders/search-profiles.js`, профили через `/api/tender-search-profiles`, запуск через `/api/tender-search/start`. Старые Flask-пути сохранены как алиасы, оба пользовательских запуска обновлены. Это исправление проверено отдельно; первый выпуск профилей не считался полностью подтверждённым публичной проверкой.

Проверки: полный изолированный AutoBot **545 passed, 1 skipped, 20 subtests**, 64.34 сек. После правки маршрутов — **67 passed** (профили/поиск), JavaScript syntax check и повтор браузерного открытия/запуска. Настоящий Nginx на отдельном loopback-порту с синтетическими upstream проверил новые пути: анонимные/посторонние роли запрещены, четыре разрешённые роли читают форму/API, POST передаётся AutoBot, тело не уходит в auth-проверку. Защита Origin и прежние worker Bearer-сценарии сохранены. Рабочие пользователи/сессии в тестах не использовались.

Linux-образ: профиль переживает отдельный процесс, копейки и открытая граница сохраняются, повтор идемпотентен, изменённый профиль не подменяет продолжающийся запуск; API и запрет чужого Origin проверены. Регрессионные Linux-проверки документов, OCR, рынка и очереди также прошли. Полный Python-набор не повторялся после изменения только маршрутов; проверены затронутые случаи и финальный образ.

Браузер на изолированной копии: **390/768/1280 px**, сохранение, длинное название, повторное открытие/перезапуск preview, отсутствие верхней границы, ошибочный диапазон без запуска, раскрытие лимитов, доступные кнопки. Запуск передал тестовому исполнителю те же регионы/темы/копейки/давность/лимит; настоящая загрузка и Telegram отключены. Горизонтального переполнения нет, viewport сброшен, ошибок консоли нет. Финальная поправка пути повторно проверена в браузере; CSS после матрицы размеров не менялся.

После окончательного выпуска: Git/образ совпали, health, внутренние реальные HTML/JS/API и их байты, запрет анонимных новых URL, существующие защищённые API, запрос CRM к AutoBot, лидер web-исполнителя и пустая активная очередь проверены. 11 SQLite integrity ok/FK 0. Публичный сценарий под настоящей пользовательской сессией в этом выпуске не выполнялся; разрешённые роли/проксирование проверены изолированным Nginx, обработчики — реальным образом. Самопроверка diff выполнена; независимого ревью не было.

Отдельная read-only проверка ЕИС из финального на тот момент образа профилей, без подключения рабочих данных: 15 сентября, 10:42 UTC, один запрос «Москва / ремонт школы», срок 30 секунд. Навигация завершилась TimeoutError через 30,4 сек, страниц и карточек 0. Это ошибка доступности источника, а не доказательство отсутствия подходящих закупок. Новые профили не объявляются исправлением внешнего таймаута. Логи: `profile-bot-*.log`, `profile-route-bot-*.log`, `profile-live-eis.log`, `nginx-access-probe.log` в локальном tmp.

Далее — компактный каталог и соответствие главного действия реальному состоянию карточки, включая аудит остальных путей через production-прокси. Общая приёмка реальных цен, внешний Avito, оставшиеся поля OCR, аварийное восстановление публикации и денежный график A6 продолжают оставаться открытыми.


## A7 — компактный каталог, следующие действия и поиск позиции опубликованы 15 сентября

Bot **`006a87f66ad56e8ec8f26da3dfa294b120dbe980`**, образ `crm-autobot:release-006a87f66ad5`, SHA `eb979b418b3d318f0a0bdd430b7258918f47440cae87c75266816819e68f6d4f`. Исходные Bot `97e6316` и CRM `4577977`, пользовательский checkpoint сохранён без изменения. Backup `/opt/crm-backups/autobot-interface-20260915T112736Z`, rollback `crm-autobot:rollback-97e6316c3dc8`; 11 SQLite проверены восстановлением, FK 0. Рабочие данные не мигрировали; CRM продолжает работать на `e4f64aa`, действующий Nginx не изменён.

Каталог стал компактнее: первое действие и несколько карточек видны при 1280×900. Добавление по ссылке, заказчик, даты и наличие файлов раскрываются по запросу. Убраны повторные заголовки и пустой блок состояния; ошибки, прогресс и журнал остаются доступны. Сохранены все фильтры, сортировка, поиск по скрытым сведениям, групповой выбор, выгрузки, импорт в CRM и удаление с корзиной. «Есть сравнение» заменило обещание готовности к решению; наличие файлов не подтверждает цену или прибыль. Технический `is_ready` и прежние фильтры сохранены для совместимости.

Незавершённые журналы загрузки/разбора имеют приоритет над старыми отчётами в каталоге. Один download_log или временный входящий файл не считается документом. Основная кнопка запускает отдельное скачивание либо разбор, когда нужен соответствующий этап. Карточка выбирает действие по комплекту, ошибкам архива/разбора, пропускам количества/единицы/суммы, подтверждённым ценам: загрузить, разобрать, проверить позиции, найти цены или открыть сравнение позиций. Прежний отчёт и ручные действия остаются доступны. Пустая смета больше не предлагает запуск рынка второй несогласованной кнопкой.

Проверка маршрутов выявила ещё неработающие за production-proxy адреса. Клиент теперь использует `/api/reports/rebuild`, `/api/reports/rebuild-all`, `/api/tenders/storage-overview`, `/api/tenders/crm/projects`, `/research/items`, `/tenders/market-audit`; старые Flask URL остаются алиасами. Права и обработчики прежние, встроенный выбор объекта по-прежнему использует защищённый CRM-мост. Реальный Nginx на отдельном порту с синтетическими upstream проверил запрет анонимных/посторонних ролей, доступ четырёх разрешённых ролей, путь/тело POST, Origin и отдельный worker Bearer. Производственная конфигурация сверена по SHA и не расширялась.

Проверка итогового JavaScript обнаружила прежнюю синтаксическую ошибку страницы «Поиск позиции»: Python превращал `\n` внутри примера в настоящий перенос строки JavaScript-строки, ломая весь скрипт. Скрипт вынесен в `static/research.js` и отдаётся по защищённому `/research/client.js`. Повтор Ctrl+Enter не создаёт параллельный запрос; ссылки источников допускают только HTTP/HTTPS, кандидаты не показываются принятыми ценами. Пример из двух строк, пустой ввод, ошибка/повтор, безопасный вывод и недопущение повторного запуска проверены исполняемым Node-набором.

Проверки:

- Сначала 77 затронутых Python-тестов; после окончательных правок **558 passed, 1 skipped, 20 subtests**, 58.47 сек, изолированный runtime с запрещённой сетью. Полный набор AutoBot, а не всей CRM. После этого удалены только одинаковые повторные CSS-правила, раскладка повторно проверена.
- `node tests/research_frontend_tests.js` прошёл; `node --check` проверил четыре итоговых inline-скрипта пяти страниц, отдельный research.js исполнен Node-набором. Первое падение проверки скрипта research исправлено; оно не скрыто обновлением ожиданий.
- Linux-образ без рабочей БД, секретов и внешней сети: реальный Excel, выбранное действие, повтор после ошибки, один основной элемент, экранирование, новые маршруты и JS. Существующие проверки OCR, архивов, загрузок, публикации, очереди, источников и профилей также прошли.
- Браузер **390/768/1280 px** на копии: каталог и длинная карточка, фильтр закона (8 из 48) и сброс, поиск по номеру, заказчик, меню с выгрузкой/CRM/удалением, выбор одной карточки и отмена, ошибочная ссылка без запуска, хранилище. Первые карточки каталога начинаются примерно на 520/423/352 px; на телефоне основная кнопка длинной карточки доступна около 765 px. Переполнения страницы нет.
- На синтетических сметах проверены состояния «нужны цены», пропуск количества с переходом к позициям, ошибка разбора и повтор. Браузер передал тестовому исполнителю `--from-downloaded-tender-id` с нужным номером; видимое сообщение об ошибке возвращается, старый отчёт сохранён. Реальных загрузок и сообщений не запускали. Три проверочных тендера и их файлы удалены из изолированного preview, viewport сброшен. На странице исследования работают пустая проверка и двухстрочный пример; новых ошибок консоли нет.

После публикации проверены точный Git/образ, health, реальные HTML/JS/CSS, семь защищённых путей, запрос работающей CRM к AutoBot, лидер web-исполнителя и отсутствие активных заданий. 11 SQLite integrity ok/FK 0. Авторизованный сценарий в настоящей production-сессии не выполнялся: роли/проксирование проверены отдельным Nginx, обработчики — финальным образом. Общая CRM Python/frontend-база не повторялась, её код не менялся. Самопроверка diff проведена; независимого ревью не было.

Этот пакет завершает каталог и выбор действия; качество полной реальной выдачи, внешний Авито-исполнитель, оставшаяся проверка OCR и денежный график остаются открытыми. Следующая техническая граница — восстановление согласованного набора отчётов после аварийной остановки процесса между заменами файлов.


## A4 — восстановление публикации после остановки процесса опубликовано 15 сентября

Bot **`2a3d06d527d933be3ac1843615dbb00e7b321e06`**, образ `crm-autobot:release-2a3d06d527d9`, SHA `0ef8be4d9e95b72c75babce48d0c65c0d02fd3d16f66a71c2531db1d75d6e2c0`. Исходные Bot `006a87f`, CRM `ab059f3`; исходный diff CRM пуст, пользовательский checkpoint сохранён с прежним SHA. Backup `/opt/crm-backups/autobot-publication-recovery-20260915T120941Z`, rollback `crm-autobot:rollback-006a87f66ad5`; 11 SQLite integrity ok/FK 0. Рабочие БД не мигрировали. CRM-код/образ остаётся `e4f64aa`, production Nginx не менялся.

Перед заменой четырёх файлов отчёта сохраняются предыдущие копии, SHA старого/нового содержимого и фаза публикации. Прерывание до `committed` возвращает согласованную прежнюю редакцию; после `committed` новая редакция сохраняется. При первом отчёте удаляется только незавершённый собственный результат. Восстановление не расходует резервные копии до завершения, поэтому безопасно переносит повторную остановку. Повреждённые журналы, ссылки, изменение цели/копии или неизвестные файлы сохраняются для проверки. Поздняя ошибка синхронизации/очистки сохраняет завершающий журнал для следующей попытки. Старые каталоги без такого журнала не удаляются.

Восстановление запускается перед исполнителями Gunicorn и локального сервера, а также при следующем чтении. Карточка, источник экономики, выгрузки, CRM-экспорт, сравнение и рынок разделяют существующую блокировку отчёта. Сетевой поиск не держит её; публикация сверяет версию под блокировкой. Вложенное чтение не блокирует само себя. Короткие идентификаторы старых CLI-отчётов сохранены; новый протокол применяется к номерам тендеров 8–25 цифр.

Занятый отчёт даёт 503/Retry-After, неразрешённое восстановление — 409 и понятную причину. Экран ошибки не содержит финансовых строк; оригиналы по-прежнему скачиваются и просматриваются. Статус/отмена заданий доступны. Частные копии и журналы не выдаются общим маршрутом отчётов. Каталог проверяет статус до признаков наличия файлов, чтобы видеть результат первого восстановления.

Проверено:

- Полный изолированный AutoBot: **581 passed, 1 skipped, 20 subtests**, 66.63 сек. После двух уточнений восстановления/очистки — **44 passed**, 41.35 сек, включая два новых сценария. Полный набор после этих двух локальных правок повторно не запускался; окончательный Linux-образ проверен отдельно.
- Настоящие отдельные процессы: прерывание после каждой из четырёх замен, после фиксации, посреди восстановления и после записи его статуса, повторное восстановление, первая публикация, конкурирующий читатель. Повреждение журнала/идентичности/резервной копии и внешняя правка не перезаписываются. Обычный и повторный отказ диска, повторный полноценный Excel-разбор, ошибка очистки и поздняя внешняя правка прошли.
- Первое обновлённое ожидание теста отказа восстановления уточнено по поведению: повтор одинакового HTML не требует перезаписи. Теперь тест меняет исходную позицию и действительно проверяет отказ восстановления разных байтов. Два первоначальных падения полного набора по коротким legacy ID устранены сохранением прежнего контракта; ожидания этих тестов не менялись.
- Браузер **390/768/1280 px**: ошибка и временная занятость отчёта, длинное имя оригинала, доступность кнопок и повтор карточки; переполнения нет, консоль чистая. Синтетический тендер и файлы удалены из изолированного preview, viewport сброшен. Скачивание неизменённого оригинала и блокировка финансовых HTTP-потребителей проверены API-набором.
- Окончательный Linux-образ без рабочей БД, секретов и внешней сети: настоящий SIGKILL на границах публикации/восстановления, сохранение завершённой редакции, повтор, первый отчёт, отказ ссылки на чужой каталог, восстановление через HTTP и блокировка только проблемного тендера. Существующие проверки реального Excel, PDF/OCR, архивов, загрузок, очереди, профилей, экономики и интерфейса прошли в этом же образе.

После публикации проверены точные Git/образ, CSS/HTML/JS, защищённые маршруты, связь работающей CRM с AutoBot, лидер web-исполнителя, отсутствие активных заданий и ожидающих журналов публикации, 11 SQLite. Авторизованный production-сценарий в браузере и отключение питания/носителя не эмулировались. На Windows нет fsync каталогов через используемый API: доказана остановка процесса, а не произвольная потеря питания. Остановка до создания журнала может оставить частный временный каталог, но не смешивает действующие файлы. Общая тестовая база CRM не запускалась, её код не менялся. Самопроверка diff выполнена; независимого ревью не было.

Далее — денежный график сценария A6. Доступность ЕИС, внешний Авито-исполнитель и приёмка качества на широкой реальной выборке остаются открытыми; данный выпуск их не подтверждает.

## A6: денежный график условий участия, 15 сентября

Исходный CRM `136c253`, AutoBot `2a3d06d`; первоначальные diff и SHA сохранены в локальном `cashflow-baseline`. Пользовательский checkpoint AutoBot сохранён побайтово. Новый график — необязательная часть существующего JSON версии сценария; DDL и миграции рабочих записей не требуются.

Руководитель задаёт доступные к началу средства, поступления заказчика и оплаты расходов с НДС по датам. Отдельные типы строк — уплата и возврат НДС. Суммы и налоговые сроки не выводятся автоматически. Возвратное обеспечение содержит сумму, даты внесения/возврата; комиссия остаётся обычным расходом. Пустые поля остаются неизвестными, явный ноль допускается, предустановленных процентов/денег/сроков нет. Основная прибыль считается независимо; график не создаёт фактических проводок и оплат CRM.

Для численного кассового разрыва нужны актуальные полные условия, заполненные суммы НДС, все даты/суммы графика, сверка с полными поступлениями/расходами и подтверждение календаря. Показатели: нехватка средств, первый день разрыва, конечный остаток и таблица остатков на конец дня. Движения одной даты суммируются; внутридневная очерёдность и отдельные заёмные поступления не моделируются. Неполный график сохраняется как черновик. Возврат до внесения отклоняется, введённые поля остаются в форме.

График свёрнут в существующих условиях и сохраняется общей кнопкой. История, копирование базового сценария, точные копейки, запрет для прораба, версии и повтор операций сохранены. Старый клиент, не знающий поля `cash_flow`, не удаляет прежний график; при изменении прочих условий/источника его подтверждение снимается. Явный `null` убирает график новой записью, сохраняя историю. Подробный контракт — в `docs/AUTOBOT_TENDER_SCENARIOS.md`.

Локальная проверка:

- **21 Python-тест экономики** и новый frontend-набор прошли (`tmp/checks/20260915-174132-895881`): разрыв в одну копейку, поздняя оплата, обеспечение/комиссия без двойного счёта, НДС, одинаковые даты, неизвестные поля, строгие ограничения, наследование старого JSON, повтор, конфликт, роли.
- Общий прогон CRM `tmp/checks/20260915-174238-61d32c`: **479 Python-тестов прошли**, один модуль не загрузился (`test_repair_estimate_prices_from_rows`: встроенный Python не включал изолированный рабочий каталог в `sys.path`). Исправлен только локальный запуск проверок; **все 6 тестов модуля прошли отдельно** в `tmp/checks/20260915-174854-1b73d8`. Полный прогон единым повторным запуском не проводился. **50 frontend-наборов прошли**. После прогона менялись только размеры/подписи таблицы и документация; финальные стили проверены визуально.
- Браузер **390/768/1280 px** на синтетическом тендере: сохранение/перезагрузка, ошибка даты и исправление, пустая сумма и черновик, длинное назначение, восстановление истории и независимое копирование сценария, сохранение с телефона. На проверочных суммах прибыль 49,98 ₽, разрыв 18,01 ₽, конечный остаток 69,99 ₽; все строки сверены вручную. Для прораба нет финансовых полей в DOM. Консоль чистая; страницу не распирает, таблица прокручивается внутри блока. Тестовый тендер/файл/версии удалены из изолированной БД, viewport сброшен.

Самопроверка diff выполнена: прибыль и прочие финансовые сценарии не изменены, доступ проверяется прежде чтения источников, ключи повторов и история сохранены, рабочие записи не создавались. Независимого ревью не было. Подготовлена резервная копия обеих систем `/opt/crm-backups/autobot-cashflow-20260915T124856Z` с проверкой восстановления SQLite и сохранёнными работающими образами. Сначала публикуется CRM, затем AutoBot с новой версией ссылок на CSS/JS; итоговые коммиты и проверка production будут записаны после выпуска.


Опубликовано: CRM **`b5f0b72d37a38bb10629ea6fff915131913a7bd0`**, AutoBot **`2a223f47c9815f226ffd516e9ef78dcf185e2467`**. Образы `crm-crm:release-b5f0b72d37a3` (SHA256 `19edd363a30f6de031cb81f03bf1bf50da87380254829925648e91c769542990`) и `crm-autobot:release-2a223f47c981` (SHA256 `db1fa67b8c782f9fec7a44b4183fe64f10757a5f1205698c0c4b58be8b14a21f`). Откат: `crm-crm:rollback-136c253fc475` хранит прежний работающий CRM-образ `e4f64aa`; `crm-autobot:rollback-2a3d06d527d9` — прежний AutoBot. Имя CRM rollback-тега соответствует checkout с документацией, а не старой метке образа.

В окончательном Linux-образе прошёл **21 тест экономики** без внешней сети/рабочей БД. Первая проба Bot-карточки использовала отсутствующий синтетический тендер и получила штатный 404; проба исправлена заданием синтетических метаданных, после чего проверены 200 и обе новые ссылки CSS/JS. Код приложения из-за этой ошибки пробы не менялся. Локально **17 проверок AutoBot** источника экономики/следующего действия прошли; полный AutoBot-набор не повторялся, его изменение в этом выпуске — только две версии ресурсов в шаблоне.

Production: точные Git и метки обоих работающих образов, публичные байты CRM JS/CSS, ссылки в Bot HTML, 401 на закрытых GET/POST, связь CRM → AutoBot, живой лидер web-очереди и отсутствие активных заданий/незавершённых публикаций. Проверены 4 CRM SQLite (4 существовавших нарушения внешних ключей, без изменения) и 11 AutoBot SQLite (нарушений нет); `integrity_check` везде `ok`. Nginx не менялся. Рабочие экономические условия не записывались; авторизованный production-сценарий в браузере не выполнялся. Локальное завершение коммита однажды потребовало явно добавить каталог служебного скрипта в Python path; оба итоговых commit отправлены и проверены в GitHub, checkpoint сохранён.

Далее — сохранность принятого задания при перезапуске web-процесса. Доступность ЕИС, отдельный Авито-исполнитель, полное OCR и широкая реальная выборка рынка остаются открытыми.


## A4 — сохранённые задания поиска и документов, 15 сентября

Исходный CRM `e6536ad`, AutoBot `2a223f4`. CRM diff пуст; единственное исходное изменение Bot — пользовательский checkpoint, его SHA сохранён. Приём теперь записывает задание и ограниченный журнал в новый отдельный `main_jobs.sqlite3`; основной CLI работает в отдельном процессе с блокировкой ОС. Очередь до старта восстанавливается, живой исполнитель переживает завершение web-процесса, потеря исполнителя даёт `interrupted` без самоповтора. Массовый разбор использует принятый список тендеров, статусы и URL прежних API сохранены. Повтор HTTP с номером операции не создаёт второе выполнение.

Карточка восстанавливает точный запуск, показывает отдельный журнал загрузки/разбора, ошибку и доступный повтор. Каталог сохраняет номер операции при потере ответа, разделяет последние журналы документов и сравнения, переносит длинный статус. Изменены существующие файлы стилей, без добавления слоёв `!important`. При просмотре diff найден и исправлен пропущенный потребитель: после перезапуска проверки запуска сравнения/удаления должны читать новое хранилище, а не пустой `parse_state`. При недоступном хранилище эти действия блокируются. Это самопроверка, не независимое ревью.

Проверено локально: исходные 90 проверок затронутой границы; первый полный прогон 615 passed / 1 skipped / 20 subtests; после проверки соседних действий — полный прогон **625 passed / 1 skipped / 20 subtests за 71,18 с**. Два Node-набора проходят: потеря ответа, неоднозначный ответ, явный отказ, другой тип действия, повторное открытие, конкретный `run_id`, ограничение опроса, безопасный текст журнала. Четыре процессных проверки действительно завершают web-наблюдатель/исполнитель и повторно открывают хранилище; CLI заменён локальной операцией, без ЕИС/уведомлений. В раннем конкурентном тесте выявлена гонка включения WAL, исправлена блокировкой создания схемы. Одна прежняя проверка создала mocked queued-запись в изолированной копии проекта; добавлен отдельный DATA_DIR, запись архивирована и удалена после проверки отсутствия исполнителя. Рабочие данные не затронуты.

Браузер на копии: карточка и каталог 390/768/1280, длинные статусы, раскрытый журнал, запрет второго нажатия, скачивание и повторный разбор на заглушке, перезагрузка при выполняющемся задании, последующий отказ/повтор. Горизонтального переполнения страницы нет; консоль без ошибок. Исправлено обнаруженное обрезание причины ошибки в каталоге на 390 px. Синтетическая запись и её файлы после QA удаляются с проверкой исходных данных. Реальные поиски и отправки уведомлений не запускались.

Границы: один основной запуск, без расписания/приоритетов/автоповтора прерванного CLI; старый объединённый сценарий сравнения остаётся со своим состоянием, Gunicorn по умолчанию один. Полный OCR и доказанная приёмка рыночного поиска остаются в плане. Контракт и эксплуатация — `auto_bot/docs/MAIN_JOBS.md`. Резервная копия перед выпуском: `/opt/crm-backups/autobot-main-jobs-20260915T134910Z`, 11 SQLite с проверенным восстановлением и нулём FK-нарушений; откат `crm-autobot:rollback-2a223f47c981`. Запись результата Linux/production добавляется после фактической проверки.


Пакет опубликован как AutoBot `f1e7aa45f6541e0ec6fdce81f4de7543b73f1d69`, образ `crm-autobot:release-f1e7aa45f654`, SHA `77a7daf4ebd59bf75e313d25744c33866fa964958b3e564565ca369157c8f787`. В Linux-образе с `--network none`: четыре настоящих процессных сценария, сохранённый приём/повтор, конкретный статус, следующий запрос, блокировка соседнего сценария и удаления после потери памяти web-процесса, HTML карточки/каталога и точные байты JS прошли. Проверки используют изолированные файлы и локальную операцию вместо настоящего поиска.

Production: Git и образ совпадают; шесть анонимных GET/POST получают 401, новый JS доступен по существующему защищённому маршруту, обе страницы содержат нужный код, опубликованный CSS совпадает. Перед перезапуском подтверждено отсутствие основных и рыночных заданий; после старта серверный исполнитель рынка держит свою блокировку. 11 SQLite — integrity ok, FK-нарушения отсутствуют; незавершённых журналов публикации нет. CRM продолжает получать версию источника; её работающий образ `b5f0b72` не менялся. Nginx не менялся. Реальных запусков ЕИС, рыночного сбора, записей экономики и аутентифицированной браузерной проверки в production не было. Новый `main_jobs.sqlite3` в production ещё не создан — проверка не оставляла тестового задания. Локальная синтетическая карточка, её отчёт и отдельная QA-БД удалены с проверкой исходного индекса; viewport сброшен.


## A4 — ограниченный разбор ручных загрузок, 15 сентября

Исходный CRM `9911d21`, AutoBot `f1e7aa4`; исходные diff, файлы и SHA пользовательского checkpoint сохранены. Новый путь `run_uploaded_parser` помещает существующий Excel/PDF/OCR-разбор в общий дочерний процесс с лимитами входа, времени и результата. Прогресс передаётся через ограниченный JSON. Родитель повторно проверяет SHA до записи; `position_id`, исходная единица/количество, код и диагностика проходят сериализацию, `estimate_version` и `source_sha256` привязаны к файлу. Старые записи не мигрируют.

Через настоящую форму на изолированных файлах обнаружено: обычный Excel с «Количество / Цена / Сумма» выбирал позиционный разборщик ЛСР, терял единицу/количество и записывал цену как общую сумму. Добавлен выбор явных колонок до ЛСР. Сохраняются все листы такого формата и физические идентификаторы строк; одинаковые работы на разных листах не сливаются. Повторные заголовки/итоги исключены, «Люк» и другие короткие названия не теряются, символ № сохраняет исходный номер. Смешанная книга с нераспознанным непустым листом отказывается от частичного сохранения. Прежние специализированные форматы остаются для книг без явных таблиц; универсальная полнота произвольных макетов не заявлена.

Проверки: исходные 15 тестов затронутого сценария; 15 новых проверок ограничений, настоящего дочернего Excel-процесса, таймаута и следующего запроса, повреждённого файла, сохранности SHA/ID/неизвестных значений и диагностик. Итоговый полный прогон на окончательном коде — **640 passed, 1 skipped, 20 subtests за 78,20 с**. Пропуск — прежняя недоступность создания каталоговой символьной ссылки на Windows. В раннем прогоне отсутствовал PyMuPDF только в частном тестовом runtime; подключена существующая изолированная копия той же версии, что в образе. Новых зависимостей приложения нет. Diff просмотрен отдельно: найдены/исправлены потеря коротких имён, повторный заголовок и №; это самопроверка.

Реальная копия шестистраничной PDF-сметы прошла новый дочерний путь в Linux без сети: 188,34 с, 23 события прогресса, 15 позиций с идентификаторами; суммы всех 15 совпали с ранее выполненной ручной сверкой. Частные суммы и эталон остаются в локальных QA-файлах. Названия, коды, коэффициенты и полный ресурсный состав этим не приняты. Итоговый Linux-образ отдельно проверил оба листа Excel, сохранность полей, короткие названия, прогресс, таймаут/следующий запрос, смешанную книгу, предел PDF-страниц, сохранение версии и отказ при изменении источника.

Браузер: загрузка правильного и повреждённого Excel, явная ошибка без новой карточки, повторное открытие результата; 390/768/1280 без переполнения страницы, длинное название читается, кнопка загрузки доступна; консоль без ошибок. Использована отдельная папка `upload-preview` в изолированной копии, реальные пользовательские записи не изменялись. Запусков рынка/CRM-импорта из формы не было. Внешний вид самой карточки ещё требует запланированного упрощения.

Выпуск: AutoBot `1457fa82bfbcc2cdeb2a8a0d7cd93d7c570afadc`, образ `crm-autobot:release-1457fa82bfbc`, SHA `e375d933039415530aad9ef074b5b41f99230c0138b4cd1f45c2ae0971f7a895`. Backup `/opt/crm-backups/autobot-upload-parser-20260915T143110Z`, 11 SQLite с проверенным восстановлением и нулём FK-нарушений, исходники/данные; откат `crm-autobot:rollback-f1e7aa45f654`. До перезапуска подтверждены отсутствие основных/рыночных заданий, незавершённых ручных загрузок и дочерних парсеров. После выпуска: точные исходники в контейнере, три реальных маршрута загрузки/статуса/списка закрыты для анонимного доступа, CRM получает источник AutoBot, web-исполнитель рынка держит блокировку, 11 SQLite integrity ok и без FK-нарушений, незавершённых публикаций нет. Работающий CRM-образ `b5f0b72` и Nginx не менялись. В production тестовые сметы не загружались, рабочие данные не пересчитывались. Следующий пакет: публикация rows/meta/index и межпроцессное восстановление ручной загрузки.


## A4 — атомарное сохранение и восстановление ручных смет, 15 сентября

Исходный CRM `bfc1068`, AutoBot `1457fa8`; diff пуст кроме прежнего пользовательского checkpoint Bot. Исходники, manifest и SHA зафиксированы. Новая публикация использует стандартный SQLite, таблицу `uploaded_estimates` и одну транзакцию для метаданных, строк и каталога. Проверка чтения не создаёт новую БД; старые JSON сохраняются и читаются. Это явное изменение внутреннего формата будущих загрузок без переноса рабочих записей. URL, JSON API и потребители сохранены. Метаданные рынка обновляются отдельно и не могут менять исходную смету; legacy-индекс при удалении защищён общей блокировкой и атомарной записью.

Приём сохраняет задание до подтверждения запуска, при ошибке диска возвращает 503 и не запускает OCR. JSON прогресса имеет уникальный временный файл, fsync и ограничение чтения 64 КиБ; активные задания не выталкиваются из памяти лимитом последних 16 записей. Весь исполнитель и его конечная ошибка защищены блокировкой ОС. Статус перечитывает данные с диска; другой наблюдатель не запускает второй парсер. Сохранённая SQLite-запись восстанавливает «Готово» без повторной обработки, старые метаданные/строки не считаются доказательством успеха. Проверяются папка и SHA источника, восстановление без результата ограничено тремя попытками.

Проверено: 48 первых целевых тестов; после самопроверки исправлен ещё один переход — ошибка запуска потока наблюдателя не должна перезаписывать прогресс чужого живого исполнителя. Ошибка выполнения теперь записывается внутри блокировки. Проверены успешный повтор/конфликт версии, нулевое побочное действие чтения, повреждённая БД → 503, нечисловые суммы, сохранность исходника, действующие JSON-файлы, каталог/карточка/Excel, защищённый CRM payload и удаление новой сметы без изменения прежних файлов. Старые тесты чтения конкретного `meta.json` заменены проверкой опубликованной записи с сохранением проверок диагностик и чисел; отдельный тест доказывает совместимость старого формата. Ранние падения: эти два ожидания прежнего физического формата; в новом API-тесте сначала отсутствовал обязательный capability-заголовок (403 был правильным), затем проверены оба исхода; тест памяти выявил пропущенную защиту активных заданий. Последний полный прогон **662 passed, 1 skipped, 20 subtests за 89,06 с**. Пропуск прежний — каталоговая символьная ссылка Windows.

Шесть процессных проверок на Windows и в итоговом Linux-образе: принудительное завершение до commit сохраняет прежнюю смету; после commit остаются целые метаданные/строки; два одновременных сохранения не теряют запись каталога; два исполнителя одного задания вызывают один парсер; завершённый во время разбора процесс допускает следующую попытку; завершение между commit и записью статуса не повторяет парсер. В этих сценариях парсер заменён локальной функцией. Итоговый Linux-образ дополнительно запускает настоящий ограниченный Excel-парсер, таймаут/следующий запрос, PDF-предел страниц, сохранение версии и проверку смены источника, а также подготовительную выгрузку старого формата. Сеть у проверочных контейнеров отключена.

Браузер на отдельной QA-папке: правильная загрузка, новая карточка, полный перезапуск сервера preview, повторное открытие и каталог из новой SQLite-записи и двух прежних JSON-записей. Числа и единицы сохранены, консоль без ошибок. Маршруты рынка/CRM-записи через браузер не запускались; CSS и раскладка этим пакетом не менялись, прежняя проверка 390/768/1280 относится к предыдущему выпуску. Preview первоначально запущен не тем Python без pandas; затем использован прежний настроенный runtime, приложение не менялось из-за этой ошибки запуска.

Границы: автоматическое восстановление начинается при следующем запросе статуса, нового расписания нет. После гибели web-исполнителя его дочерний парсер может дожить до ограниченного watchdog, но не может опубликовать результат; восстановленный исполнитель может повторить локальный разбор. Новый независимый POST пока создаёт новую запись. После появления новых SQLite-записей слепой откат к прежнему образу скрывает их: добавлена проверяемая подготовительная выгрузка `tools/export_uploaded_estimates.py` в отдельный новый каталог вне рабочих данных. Она не меняет production; порядок остановки, backup, проверки и отдельного переноса изложен в `docs/UPLOADED_ESTIMATES.md`. Проверены содержание выгрузки, сохранность рабочих байтов и отказ для существующего/рабочего каталога назначения.

Опубликовано: AutoBot `8b40e20e153a7b7a16f35149daa630abf8ba822c`, образ `crm-autobot:release-8b40e20e153a`, SHA `c964f3964e500017ee59a25d020b8692aab51a6402f7fb418a80480df42216f5`. Backup `/opt/crm-backups/autobot-upload-store-20260915T150301Z`, 11 SQLite с проверенным восстановлением, integrity ok и нулём FK-нарушений; откат исходного выпуска `crm-autobot:rollback-1457fa82bfbc` с описанным ограничением после будущих загрузок. Перед перезапуском не было основных, рыночных и ручных заданий/парсеров. После выпуска проверены точные байты кода внутри контейнера, три защищённых маршрута, связь CRM → AutoBot, лидер поиска рынка, целостность 11 БД и отсутствие незавершённых публикаций. Новые `estimates.sqlite3` и `main_jobs.sqlite3` в production во время проверки не создавались; тестовые записи туда не попадали. CRM-образ `b5f0b72` и Nginx не менялись.


## 15 сентября: карточка загруженной сметы и обновление сохранённого рынка

AutoBot `03ae90d7a96b90a37a6f6caa4f0b7ceef24360da`, исходный `8b40e20`. Удалён действующий inline-компонент примерно на 990 строк из web_ui: HTML/CSS/JS принадлежат карточке, старые страницы и их стили сохраняются. Сверка открывается при расхождении, журнал и разрез типов сворачиваются; полезные фильтры, исходные единицы, итоги листов, CRM и выгрузки сохранены. Оригинал проверяется по собственной папке записи и выдаётся вложением без кэширования; посторонний путь, неподдерживаемое расширение, исчезнувший файл и символьные ссылки закрыты.

Обновление рынка сравнивает версию сохранённых файлов и ждёт окончания поиска. Наличие строк-заглушек больше не означает готовую таблицу. Новый результат обновляет страницу один раз с прежними q/types; неудачный повтор со старыми файлами её не обновляет. Сохранённый рынок распознаётся после перезапуска web-процесса. Сеть/ошибка поиска видны, кнопка восстанавливается после следующего успешного статуса. Исходный источник с NaN больше не получает подпись «Есть источники». Кандидатная цена сохраняется в источниках и их Excel, но общий строгий контракт сравнения не изменён; browser QA показал 5400 во вкладке источников и 0 из 3 подтверждённых позиций в сравнении.

Проверки: итоговый полный Python 673 passed / 2 skipped / 20 subtests за 89,64 с. Пропуски — проверки символьных ссылок на Windows; новый отказ по ссылке проверен в итоговом Linux-образе. Первоначальный полный прогон обнаружил два устаревших теста, читавших перенесённый JS в web_ui; ожидания переведены на новый файл с проверкой подключения в шаблоне. Дополнительно Node исполнил сценарий импорта: локальный GET с capability и same-origin, запись через мост родителя, без прямого POST из embedded UI. Изолированный Linux-контейнер без сети проверил настоящие Flask-страницу/конфиг/ассеты, сохранённые файлы, версию, исходные единицы, Excel, кандидат против подтверждённой цены и запреты исходника. Самопроверка diff выполнена; независимого ревью не было.

Браузер на синтетической QA-смете: 390/768/1280 px, длинное название, отсутствие/частичный рынок, переход на новую версию без ручной перезагрузки, пустой фильтр, сохранённые файлы после перезапуска preview, ошибка повторного поиска и остановка, окно CRM при выключенном standalone-импорте, Escape и возврат фокуса. Скролл документа не превышает ширины окна; широкая таблица прокручивается внутри. Консоль без ошибок. Оригинал получен через действие скачивания файла; ожидание универсального download-event в in-app браузере сначала истекло, специализированное действие скачивания прошло. Реальная запись в CRM через браузер не выполнялась: мост проверен исполняемым Node-сценарием и существующими API-тестами, production-данные не использовались для UI-экспериментов.

Образ `crm-autobot:release-03ae90d7a96b`, SHA `6182dfd4ecab44f4380235003e3821aaa39594ff759dad0e2d6ac0ff81fcab9f`. Backup `/opt/crm-backups/autobot-estimate-card-20260915T154643Z`, rollback `crm-autobot:rollback-8b40e20e153a` совместим с текущим хранилищем без преобразования записей. Проверены прежний сервер/образ/Nginx, отсутствие активных основных, ручных и рыночных заданий перед перезапуском, байты кода и защищённых ассетов в работающем образе, 401 на четырёх публичных URL, CRM → AutoBot, лидер поиска рынка, integrity и FK 11 существующих SQLite. Новые хранилища загрузок/основных заданий и тестовые записи в production не создавались. CRM-образ `b5f0b72` и конфигурация Nginx не менялись. Пользовательский checkpoint сохранил SHA `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8`.

Граница: ручной рыночный исполнитель всё ещё хранит активное задание в памяти; новая версия UI не делает его выполнение переживающим перезапуск. Повтор отдельного POST загрузки пока создаёт новую запись. Ручная коррекция полных OCR-наименований/кодов/коэффициентов и подтверждение качества на 150–200 размеченных позициях остаются открытыми.


## 15 сентября: загрузка ручной сметы без дубля при потере ответа

AutoBot `e9765594f97c3dcf8932b379e42866f3a036d515`, исходный `03ae90d`. Необязательный operation_id сохраняет совместимость старых клиентов; новый браузер фиксирует UUID до первого POST, затем проверяет заранее известное задание. Одинаковый ключ/файл/имя/название возвращает прежнее задание, конфликт даёт 409, повтор после удаления принятого исходника — 410. Ошибочный или завершённый разбор не запускается повторно тем же ключом. Явная новая загрузка создаёт новый ключ.

Приём использует существующие JSON-задания: неизменяемая квитанция → временный файл/синхронизация/атомарное переименование → задание → запуск. Блокировка ОС сериализует приём одной операции; выполнение парсера сохраняет прежнюю отдельную блокировку. После остановки до файла требуется тот же исходник; после файла статус восстанавливает задание с проверкой SHA. Убираются только собственные незавершённые временные файлы. Невалидный JSON, неверная структура и подмена привязки не исправляются догадкой. Ожидаемый SHA проверяется перед разбором. Новых таблиц и переноса рабочих записей нет.

Из inline-каталога вынесен контроллер загрузки. Удалены растянутый вводный блок и повторяющие этапы плашки; название имеет явную подпись, журнал закрыт по умолчанию. Потеря ответа, недоступный статус, повтор и новая смета имеют отдельные действия. Успех открывает готовые позиции; ошибка сохраняет защищённую ссылку на исходник. Процент ошибки не достигает 100, приблизительный отмечен ≈. Восстановление действует в текущей сессии вкладки; закрытие всей сессии не заменяет историю загрузок.

Проверки: итоговый полный Python 694 passed / 2 skipped / 20 subtests за 100,74 с. Два пропуска — Windows-ограничения символьных ссылок из существующего набора; Linux-проверки доступа из предыдущего пакета сохраняются. Новые целевые проверки приёма/процессов/сохранения/прогресса: 43 passed. Существующий массовый импорт: 5 passed. Node-наборы загрузки и карточки прошли; новый исполняет потерю ответа, восстановление ключа, защиту двойного submit, повтор, новый файл, терминальную ошибку и исходник. Пять настоящих процессных тестов: остановка после квитанции, до переименования временного файла, после исходника, после задания до ответа, два одновременных процесса. Они прошли на Windows и в Linux-образе без сети. В том же образе реальный ограниченный Excel-парсер вернул одну строку 100 м2 × 0,2 × 100,05 = 20,01; повтор не запустил второй разбор, конфликт не изменил байты, ошибка не создала пустую карточку. Самопроверка diff выполнена, независимого ревью не было.

Браузер на изолированных синтетических данных: 390/768/1280 px, длинные подписи, компактная форма, ошибка повреждённого Excel и доступность её исходника после перезапуска preview/обновления, скачивание, сообщение при пустом выборе файла. Искусственная потеря первого подтверждения после реального приёма: сохранённый ключ нашёл прежнее задание, обновление открыло готовую карточку; проверено одно описание приёма и одна смета по этому ключу. Длинный OCR в середине выполнения этим браузерным сценарием не проверялся. Консоль без ошибок, переполнения страницы нет; таблица прокручивается внутри. Рабочие данные для этих экспериментов не использовались.

Образ `crm-autobot:release-e9765594f97c`, SHA `b9206ff81a1989f5646e2c0ee1aaa75087e3ea9b442f7ef369c2b143d23fe752`. Backup `/opt/crm-backups/autobot-upload-admission-20260915T162232Z`, rollback `crm-autobot:rollback-03ae90d7a96b`. Прежний код понимает уже принятые JSON-задания/SQLite-сметы, но не восстанавливает незавершённую новую квитанцию и не читает новый ключ браузера: откатывать при отсутствии активных загрузок, сохраняя квитанции. До запуска проверены прежние ревизии, образ, Nginx, отсутствие активных основных/ручных/рыночных заданий и парсеров. После — точные байты кода/ассетов, 401 на загрузке/статусе/контроллере/исходнике, CRM → AutoBot, лидер web-поиска, integrity/FK 11 SQLite. Новых production-загрузок/хранилищ тесты не создали. CRM-образ `b5f0b72`, Nginx и checkpoint `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8` сохранены.

Границы: полнота OCR-названий/кодов/коэффициентов, ручные исправления с версиями, сохранность ручного рыночного исполнителя, размеченная оценка 150–200 позиций и доступность Авито/ЕИС остаются следующей работой. Условия доставки, накладных, резерва и налогов по-прежнему вводятся для каждого тендера без произвольных процентов.


## 15 сентября: ручной рынок в общей сохраняемой очереди

AutoBot `c0559825afce705afb5cc2765916449b3b76be8c`, исходный `e976559`. Удалён отдельный Flask-поток ручного поиска и его память. `uploaded_market` связывает неизменяемый план, SHA строк, физические позиции и задания существующего `agent_market_queue`. Общий web-исполнитель получает только поддержанный режим; внешние агенты не забирают ручные задания. Приём целого запуска транзакционный, клиентский UUID сохраняется до запроса; повтор возвращает тот же запуск, даже завершённый. Устаревшая остановка не затрагивает новый запуск. Смена исходника/города, удаление, отмена и замена операции проверяются до применения результата. Удаление активной сметы запрещено.

В существующей queue-БД добавляются при первой записи только две таблицы запуска/связей и индекс. Старые записи не преобразуются; простое чтение таблиц не создаёт. Лимиты поиска, lease/heartbeat/retry и принятая доставка используют общий механизм. Канонический `market_sources.xlsx` заменяется после производного сравнения, атомарно; карточка/экспорт строят сравнение по каноническим данным. Принятый ответ после гибели процесса публикуется без нового запроса и дублей. Нечитаемый старый рынок сохраняется для восстановления. Кандидаты не участвуют в расчёте; отсутствие цены явно отражено в строке.

Сохранены URL API, фильтры, вкладки, Excel, исходник, единицы, регион и импорт CRM. В карточке указана фактическая доступность источников: сайты поставщиков/подрядчиков, Авито ещё не подключён. Состояния ошибки/отмены отделены от успеха; кнопка остановки читаема на телефоне. В браузере обнаружена прежняя ошибка `nan`-ссылок в сравнении: очищены пустые значения и ссылки из старых колонок/JSON ограничены http(s). Новая проверка сначала выявила строковое `nan`; после исправления прошла, ожидание не ослаблялось.

Полный Python: 725 passed / 2 skipped / 20 subtests за 105,84 с. Два исходных Windows-пропуска символьных ссылок сохранены. Node карточки проверил потерю ответа, ключ после перезагрузки, двойной submit, точную отмену, вкладки/импорт. Шесть настоящих процессных сценариев прошли на Windows и в итоговом Linux-образе: остановка до/после commit приёма, во время lease, после принятия доказательства, после файлов до commit, два одновременных процесса. Linux API/общий исполнитель проверены без сети на синтетических подтверждённых ценах; сохранены две одинаково названные физические строки и исходная запись прежней очереди. Backup/restore новых таблиц сохранил все связи и counts, integrity/FK корректны. Самопроверка diff выполнена; независимого ревью не было.

Браузер 390/768/1280 px на отдельной БД: длинное название, состояние очереди, сохранение прогресса после убийства/restart preview, завершение 3/3, ошибка, повтор, точная остановка и сохранённые кандидаты. Искусственная потеря подтверждения дала один запуск в БД, после обновления найден прежний результат. Нет переполнения страницы, таблица прокручивается внутри; финальная консоль без ошибок. Поставщики в UI-проверке подменены, полнота реального поиска этим не доказана.

Production: образ `crm-autobot:release-c0559825afce`, SHA `f9b31bad804898e52623472efabc28910fd7c75f6b1b1adf6ccf238c511f036b`; backup `/opt/crm-backups/autobot-uploaded-market-20260915T171517Z`, rollback `crm-autobot:rollback-e9765594f97c`. До выпуска проверены прежние ревизии/образ/Nginx и отсутствие активных заданий/парсеров. После — точные байты кода/ассетов, 401 на start/status/stop/workspace.js, 11 SQLite integrity/FK, лидер web-поиска и CRM → AutoBot. Тестовые задания и новые таблицы на production не создавались; DDL применяется при первом пользовательском запуске. CRM-образ `b5f0b72`, Nginx и checkpoint `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8` сохранены.

Откат на прежний исполнитель — только без активных `estimate:*`, включая `applying`: старый код не понимает новый контекст. Историю, новые таблицы и файлы сохранять; не восстанавливать старую БД поверх новых действий пользователя. Миграции прежних рабочих записей не проводились. Следующие границы: версионные ручные исправления, полнота OCR и размеченный набор 150–200 позиций, реальная доступность ЕИС/Авито. Пользовательские условия затрат вводятся для каждого тендера без произвольных процентов.


## 15 сентября: проверка исходника и история исправлений загруженной сметы

AutoBot `60bf00e9e22a3779e8869eec120e7de395d0ef62`, предыдущий `c055982`. `uploaded_corrections` хранит неизменяемые события с автором, причиной, значениями до/после, SHA исходных строк, версией и ключом операции. Отдельная `.corrections.sqlite3` создаётся при первой правке: DDL и событие в одной транзакции, FULL-синхронизация; существующие данные не преобразуются. Первоначальные строки и файл сохраняются, исходник скачивается и при недоступной истории. Точная десятичная форма/копейки, неизвестные поля, ноль, масштабные единицы и суммы с коэффициентами не заменяются догадками. Ограничены точность, размер чисел, JSON и файлов, в том числе нулевая экспоненциальная запись огромного порядка.

Новая граница `uploaded_review` обслуживает форму, API, историю и ограниченный просмотр исходника. `crm_actor` проверяет действующую сессию через настроенный CRM `/api/auth/me`, передавая только Cookie/Authorization; не использует сервисный логин, не следует redirect и не берёт автора из тела. Роли main_admin/admin/director/foreman; ответы о правках private/no-store. Автор хранит только id/имя. История выдаётся страницами по 20 без внутренних переопределений. Клиент сохраняет ключ и тело до POST, восстанавливает подтверждение после перезагрузки, сохраняет поля при конфликте/ошибке и блокирует двойное нажатие.

Текущие строки и метаданные согласованы при чтении карточки/экспорта/CRM; первоначальный ключ строки CRM сохраняется после переименования. Для исправленных строк неполные количества/единицы/цены/суммы останавливают импорт с 422, нулевая цена сохраняется. Исправление меняет версию только своей физической строки; старый рынок без версии тоже перестаёт совпадать. Взятое в работу задание со старым SHA не может применить результат. Сохранены прежние URL, фильтры, роли, массовый импорт и остальные строки рынка.

Excel показывает исходные ячейки с прежними лимитами просмотра. Встроенный просмотр PDF оказался пустым в браузере: заменён на одну страницу PNG из существующего отдельного процесса, выбор страницы и увеличение. Лимиты 128 МиБ, 250 страниц, 3 Мп/8 МиБ на картинку, 25 секунд, Linux AS 768 МиБ, общая блокировка. Исходная страница строки открывается сразу. Оригинальный файл не меняется и остаётся доступен при отказе. Новых библиотек нет.

Итоговый Python: 769 passed, 2 skipped, 20 subtests passed in 109.49s (0:01:49). Node `estimate_review_frontend_tests.js` прошёл. Новое поведение покрыто проверками ролей/подмены автора, точных сумм и неизвестного количества, несовместимого рынка и lease, старого формата, повреждённой истории/изменённого исходника, текущего экспорта/каталога/импорта, страниц PDF и следующего просмотра после ошибки. Настоящие процессы Windows и Linux остановлены до commit, после commit и запущены одновременно: сохраняется исходная либо ровно одна новая редакция. Отдельная проверка итогового Linux-образа использовала изолированный HTTP-ответ CRM и настоящую передачу сессии, форму/API, PDF-процесс, экспорт, стабильные ключи и backup/restore всех событий.

Первый полный прогон выявил изменение метаданных документа без исправлений: возвращён прежний контракт, ожидания не ослаблялись. Самопроверка также сохранила 404 для некорректного номера документа. Самопроверка diff проведена, независимого ревью не было. Общий набор CRM в этом пакете не запускался: исполняемый CRM-код не менялся.

UI проверен при 390/768/1280 px на отдельной БД: длинные подписи, прежние/новые значения, Excel и PDF с увеличением, ошибка дробной копейки, потеря ответа после commit, восстановление по ключу, конфликт второй вкладки с сохранением введённой суммы и загрузкой текущей редакции, реальное сохранение при 390, история после restart. Три события, из них одно с искусственно потерянным подтверждением; исходные значения сохранились. Переполнения страницы нет, увеличенный PDF прокручивается внутри. Консоль финального PDF-просмотра без ошибок.

Production: `crm-autobot:release-60bf00e9e22a`, `sha256:4bbdba0bfcb3c8108e7a506edadeb22977fde383ae9a2054867a0c6bb9803c99`; резерв `/opt/crm-backups/autobot-uploaded-corrections-20260915T181434Z`, откат `crm-autobot:rollback-c0559825afce`. Подтверждены прежние HEAD/образ/Nginx, пустые активные очереди/парсеры, backup/restore 11 SQLite без новых FK-ошибок. После выпуска — байты кода/ассетов, 401 на шести новых маршрутах, отказ реальной CRM для недействующей сессии, CRM → AutoBot, лидер web-поиска, integrity/FK всех 11 БД. Исправления и тестовые записи на production не создавались. CRM-образ `b5f0b72`, Nginx и checkpoint `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8` сохранены.

Первый production probe ошибочно ожидал 401 для постороннего cookie. По backend/auth.py и server.py /api/auth/me допускает публичного посетителя; crm_actor правильно отказывает его роли с 403. Ожидание исправлено на этот фактический контракт, контроль запрета сохранён. Исходный неуспешный лог сохранён.

После первой пользовательской правки откат на код, не читающий историю, запрещён без отдельного переноса текущих значений: такой код молча покажет первое распознавание. Сохранять новую БД и исходники, выпускать совместимую версию; старую резервную БД не накатывать поверх новых действий. Следующие границы: проверка строк тендерных документов, качество OCR и размеченная приёмка рынка. Доступность ЕИС/Авито не подтверждена, настройки моделей/оплачиваемых сервисов не менялись.


## 15 сентября: порядок слов PDF, единицы и гонка открытия очереди

AutoBot `6f0de4ecce785cd27abf0ef86ab2469d69d7a877`, исходный `60bf00e`. Диагностика сохранённых наблюдений OCR показала перестановку слов нескольких печатных строк из-за сортировки всей полосы по X. Теперь слова группируются в строки с допуском наклона, затем читаются сверху вниз. Продолжение ограничено своей колонкой/страницей, следующей позицией, нормативной ссылкой, формулой объёма или ресурсной строкой. Сохраняются существенные третьи строки — площадь участка, толщина слоя. Для строки ремонта отмостки описание коэффициента перестало попадать в поисковое название. Исходный PDF не меняется, коэффициенты не вычисляются по сходству итогов.

Пропущенная единица может восстанавливаться по одному значению второго уже существующего прохода OCR в той же физической ячейке. Масштаб 100/1000 сохраняется один раз; размерность в названии, другая колонка/страница и конфликт наблюдений не подменяют единицу. Структура стандартной ЛСР с фиксированными колонками остаётся ограничением. Непрочитанные шифры, буквы и договорные коэффициенты не нормализованы произвольно.

На частной шестистраничной смете визуально проверены области страниц 2–5. Девять многострочных названий получили правильный порядок; две единицы восстановлены с печатной колонки. Все 15 сумм (109245648 копеек), количества, физические ID, исходные шифры и три вложенных ресурса сохранены. Две единицы и одно количество оставлены неизвестными. Финальный Linux-образ заново выполнил настоящий OCR и публикацию отчёта без сети на изолированных каталогах: названия совпали с просмотренными координатами, определённые единицы/количества — с ручной разметкой, все суммы и SHA исходника — с эталоном. Частный PDF и его OCR-наблюдения не включались в Git. Это проверка одной сметы, не широкая приёмка OCR.

В первом общем прогоне 783 passed / 1 failed / 2 skipped проявился отдельный прежний дефект: два процесса одновременно впервые переключали journal_mode очереди, один получал SQLITE_BUSY на PRAGMA несмотря на busy_timeout. `agent_market_queue._connect` теперь проверяет текущий режим, не переключает готовый WAL и повторяет только настройку соединения при SQLITE_BUSY/LOCKED, с закрытием неготового соединения и общим пределом 20 секунд. Повтор транзакции/задания не выполняется. Повреждённые БД и другие ошибки не скрываются. Схема и записи не мигрируют.

Целевые PDF-проверки, затем проверки очереди/реальных процессов (19 passed) прошли. Итоговый общий Python: 789 passed, 2 skipped, 20 subtests passed in 117.06s (0:01:57). Два прежних пропуска Windows сохранены. В образе без внешней сети прошли шесть настоящих процессных сценариев ручного поиска, включая два одновременных приёма и восстановление после принятого ответа, а также API/общий исполнитель/backup-restore из предыдущего пакета. Самопроверка diff выполнена; независимого ревью не было. JS/CSS/раскладка не менялись, новый прогон размеров браузера и всей CRM не выполнялся; результаты интерфейса предыдущего выпуска остаются отдельно зафиксированы.

Production: `crm-autobot:release-6f0de4ecce78`, `sha256:103d39126067a3f31b9cec5e1bc485f6148c663e0320a9975b2464932b5b2893`; резерв `/opt/crm-backups/autobot-ocr-fields-20260915T183409Z`, откат `crm-autobot:rollback-60bf00e9e22a`. Перед выпуском проверены прежние ревизии/образ/Nginx, отсутствие активных парсеров/очередей, backup/restore 11 SQLite. После — точные байты PDF-адаптера и очереди, существующие защищённые маршруты, CRM → AutoBot, лидер web-поиска и integrity/FK 11 SQLite. Рабочие документы не переразбирались, история исправлений не создавалась. CRM-образ `b5f0b72` и пользовательский checkpoint `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8` сохранены.

Следующие проверки: исправления тендерных строк, больше реальных размеченных смет/рыночных позиций, текущая доступность ЕИС и внешнего Авито. Пользовательские затраты по каждому тендеру вводятся явно без произвольных процентов.

Дополнительная диагностика ЕИС 15 сентября: 18:42 UTC, один настоящий запрос поиска из изолированного контейнера production, 30,7 с, 0 загруженных страниц, TimeoutError. Транспортная проверка 18:45 UTC: DNS 0,05 с, TCP/443 timeout 4 с; до TLS/HTTP соединение не дошло. С этого ПК в 18:46 UTC: DNS 0,02 с, TCP 0,05 с, затем SSLCertVerificationError; встроенный браузер получил ERR_CERT_AUTHORITY_INVALID. Ограничения времени проверок не равны общему SLA ЕИС. Сертификаты и настройки сети не менялись, проверка TLS не обходилась. Нельзя интерпретировать эти результаты как нулевое число тендеров или доказанную общую аварию ЕИС. Локальные журналы late-live-eis.log, eis-transport-probe.log и eis-local-transport-probe.log оставлены в изолированном рабочем каталоге.

Hermes: пользователь подтвердил Mac mini и прислал сетевое имя/пользователя. Попытка SSH с BatchMode/StrictHostKeyChecking/ограничением соединения завершилась на разрешении Mac-mini.local, до авторизации не дошла. Уточняются локальный IP и общая сеть; конфигурация Hermes и production-токен не менялись. Наличие установленного Hermes ещё не подтверждает запущенного исполнителя Авито.


## 16 сентября: исправления тендерных позиций с общей публикацией

AutoBot `4d794fcc7c44e6b5190364d6dfb3af74e63125c9`, исходный `6f0de4e`; исходные diff/HEAD/checkpoint зафиксированы до правок. Изолированная реализация добавляет `tender_corrections` и `tender_review`, расширяя прежний редактор ручной загрузки. В карточке каждой физической строки есть «Сверить с исходником». Исправляются название, шифр, единица, количество, цена и сумма. Тип остаётся за существующим классификатором. Автор берётся через CRM `/api/auth/me`, действуют прежние роли main_admin/admin/director/foreman; тело запроса не задаёт автора. Новые API/просмотр/скачивание проверяют доступ и запрещают кэширование. Путь файла берётся из проверенного состава документов конкретного тендера, включая распакованные исходники; проверяется SHA.

Для всех потребителей обновляется один канонический отчёт. История входит в ESTIMATE_PARSE JSON той же четырёхфайловой публикации: исходные значения изменённых строк, накопленные правки, события с причиной/автором/временем, версии и контрольные суммы. Блокировки комплекта/отчёта сохраняют прежний порядок, очередь не захватывается под ними. Исходники, координаты и ресурсы сохраняются. Деньги в истории — точные десятичные строки, исправленные значения дополнительно в копейках; числовые колонки Excel остаются числами с проверкой обратного чтения. Неизвестное не становится нулём, сумма не вычисляется автоматически из цены/количества. Отрицательные явно введённые значения допускаются в истории распознавания, но ограничения импорта проверяются отдельно. Формулы из пользовательского текста не выполняются.

Один operation_id связывает автора, строку, редакцию, изменения и причину. Повтор возвращает прежнее событие даже после последующих правок; конфликт ключа/устаревшая вкладка дают 409. Прерывание до commit восстанавливает прежний комплект, после commit оставляет новый; следующий запрос завершает восстановление. История ограничена 1000 событиями/16 МиБ, отчёт — 20 000 строк/500 000 ячеек; переполнение/повреждение не удаляет историю. Исправленная строка получает новую версию без смены физического ID. Старый рынок/отложенная доставка не подменяют её, неизменённые строки сохраняют совпадающие результаты. CRM-импорт сохраняет исходный source_item_key при переименовании; неизвестные единица/положительное количество/цена/сумма дают 422 до передачи в CRM. Нулевые суммы сохраняются.

Повторный разбор переносит историю только при идентичном наборе SHA и полном совпадении первоначального распознавания. Изменившийся исходник или другой разбор оставляют прежнюю публикацию. Сценарий сравнения и переноса на отличающуюся основу пока не реализован; это явное ограничение, а не завершённая приёмка всех исправлений OCR.

Проверки: 815 passed, 2 skipped, 20 subtests passed in 131.96s (0:02:11). Два прежних Windows-пропуска сохранены. Целевые core/API/импорт/публикация/экономика/ручные исправления прошли; Node `estimate_review_frontend_tests.js` проверил оба вида смет, отдельные ключи восстановления, потерю подтверждения и ссылки истории. Первые запуски новых фикстур выявили отсутствующий ParseLimits и отсутствие корня проекта в дочернем QA-процессе; исправлены фикстуры/дочерний импорт, затем целевые и полный прогоны успешны. Три настоящих процесса проверены на Windows и в итоговом Linux-образе: остановка до commit, после commit, два одновременных запроса с одним ключом. В образе без внешней сети/production-томов дополнительно проверены настоящий Excel-парсер и повторный разбор, HTTP-подтверждение CRM-автора на изолированном сервере, исходные Excel/PDF, копейки, stale-market, запрет угадывания при импорте и восстановление канонического комплекта из копии. Весь Python-набор в Linux не повторялся. Самопроверка diff выполнена; независимого ревью и полного прогона CRM не было.

Браузер на изолированных данных: редактор и карточка при 390/768/1280 px, ширина документа 375/753/1265 px без горизонтального переполнения. Длинные названия/ссылки, сохранение, исходный Excel, потеря первого успешного ответа и восстановление редакции 1 после обновления; затем конфликт двух вкладок и загрузка редакции 2. Вспомогательное сохранение количества -1 было допустимо по контракту; нечисловое abc получило 400 без изменения сметы, затем количество возвращено к 0,25 редакцией 4. После остановки/запуска preview интерфейс и независимое чтение сохранили четыре события, 25,01 руб и неизменённую соседнюю физическую строку/исходные SHA. В реальном шестистраничном PDF проверены начало с соответствующей страницы, переход 2→3→2 и форма страницы, доступность масштаба/скачивания на телефоне. Полный OCR в этом пакете повторно не выполнялся; его результат финального образа `6f0de4e` описан выше. Сообщение отсутствующего отчёта открывает документы; итоговая консоль редактора без JS-ошибок. Частные PDF/тексты и тестовые записи в Git/production не попадали. Вспомогательный HTTP-запрос из runtime с network_guard ожидаемо заблокирован; независимая проверка записи выполнена без сети, браузер проверял настоящий локальный HTTP.

Production: `crm-autobot:release-4d794fcc7c44`, `sha256:1243b78ff0f8a37bb91c804849ff6ca8544cbdf3d2b615c75d6755c762c19d1f`; резерв `/opt/crm-backups/autobot-tender-corrections-20260915T194157Z`, rollback `crm-autobot:rollback-6f0de4ecce78`. Перед переключением проверены HEAD/образ/Nginx, свободное место, 11 SQLite через backup/restore и отсутствие активных заданий/парсеров. После — 10 маршрутов с 401 для анонимного доступа, отказ реальной CRM при неподтверждённой сессии, точные байты новых/затронутых модулей и ассетов, CRM → AutoBot, лидер web-исполнителя, integrity/FK 11 SQLite. Рабочие сметы не переразбирались и не исправлялись проверками. CRM-образ `b5f0b72` и checkpoint `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8` сохранены.

Откат: предыдущий код читает исправленный Excel, но не переносит новую историю при повторном разборе. После появления реальных пользовательских исправлений нужен совместимый код либо отдельный план сохранения истории; нельзя вслепую откатывать и переразбирать документы. История не мигрирует существующие рабочие БД.

Осталось: доступный доверенный маршрут ЕИС, готовый внешний Авито/Hermes, широкая размеченная приёмка 15–20 тендеров/150–200 позиций, шифры/коэффициенты OCR и сверка отличающихся основ. Пользователь уточнил, что Hermes продолжают настраивать в другой задаче; дополнительный IP здесь не запрашивается, конфигурация не меняется. Активная задача «Настроить резервный Hermes на WSL» прочитана без отправки сообщений/поручений, её готовность не подтверждена. Настройки затрат и налогов остаются отдельными для каждого тендера без произвольных процентов. Полнота поиска и прибыль на реальных разнородных тендерах ещё не приняты.


## A3/A5 — браузерная проверка без Hermes, 19 сентября 2026

Указание пользователя: Hermes пока не подключать, попробовать обычный браузер. Исходный CRM HEAD `eda4f6e59791fc9c88b7f968f95e117ec25b110e`, AutoBot `4d794fcc7c44e6b5190364d6dfb3af74e63125c9`; сохранены `.agents/`, `.codex/` и изменённый checkpoint SHA `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8`. Работа выполнена в свежей изолированной копии; рабочие цены/сметы не использовались как тестовая БД.

Опубликован AutoBot `28fa0035f1db9d504902a0cc81a459869109dfa2`. WebBrowserFetcher подключает существующий ограниченный рендерер к queued web search: отдельный временный Chromium-контекст на позицию, без личных cookies и профиля Авито, с прежними ограничениями запросов, страниц, времени, отменой и durable delivery. При ошибке HTTP или неподтверждённой странице включается браузерная проверка. Google-discovery реализован как отдельная опция с раздельным кешем и паузой после CAPTCHA; **на production выключен**. Авито автоматически не добавляется в web-задания. Задания, результаты и статусы больше не требуют Hermes/Mac mini, при этом исторические идентификаторы адаптеров и URL API сохранены. Статусы различают встроенную web-очередь и ожидание внешнего исполнителя.

Живое исследование: Google и Авито открылись в обычном браузере ПК. В выдаче Авито найдено 25 объявлений; открытое объявление 8313664391 показывало 3 900 ₽/м³, М300 в заголовке и М200/В15 в характеристиках. Добавлена проверка, не позволяющая совпадению заголовка скрывать явное противоречие характеристик. На yaroslavl-beton.ru для М300 видны несколько цен по составам и оговорка об ориентировочной стоимости. На beton-yar.ru — 6 700 ₽/м³ для М300/В22,5, доставка отдельно. Серверная проверка в отдельном контейнере без production-данных и секретов: Google CAPTCHA; Bing загрузился, но дал общие статьи; Chromium открыл beton-yar.ru, анализатор извлёк 6 700 ₽/м³ из 83 ценовых фрагментов. В рабочие отчёты экспериментальные цены не импортировались. Продавцам не писали, заявки не отправляли, CAPTCHA не проходили. Подробности — `auto_bot/docs/BROWSER_MARKET.md`.

Проверки: исходные целевые 45 passed; затем целевые проверки поиска, очереди, единиц, спецификаций, доказательств, API и доставки. Итоговый полный набор **827 passed, 2 skipped, 20 subtests passed in 152.71s**; Node `research_frontend_tests.js` прошёл. Два прежних Windows-пропуска сохранены. В первом полном прогоне один прежний тест сравнивал CRLF HTTP-байты с нормализованным `read_text`; изменён на точное сравнение байтов файла, сохранена проверка маршрута/CSRF. Два старых ожидания Hermes заменены новым переносимым контрактом с прежними ограничениями. Самопроверка diff выполнена; независимого ревью, полного CRM-набора и повторной визуальной проверки 390/768/1280 не было, шаблоны/CSS не менялись. Полный Linux-набор не запускался; Linux проверен живыми изолированными пробами и runtime-проверкой финального образа.

Production: `crm-autobot:release-28fa0035f1db`, `sha256:d5336b0b51abafbe5fb2ecc6ff90788e70aef3475dffb9e57200ea998ef656d8`. Резерв `/opt/crm-backups/autobot-browser-20260919T103624Z`, rollback `crm-autobot:rollback-4d794fcc7c44`. Перед переключением сверены HEAD/образ/Nginx, активные задания, SQLite backup/restore; после — 6 защищённых маршрутов, точные байты 4 модулей, рабочий лидер очереди, прежние счётчики queued/completed/canceled/failed, integrity/FK всех 11 SQLite. Первая проверка завершения с невалидным ID `missing` получила 404 на CRM-прокси; после проверки контрактного формата проверка с 32 hex-символами подтвердила отказ доступа. Никакой результат задания не публиковался этим запросом. CRM-образ `sha256:07ec60c0756da9f021b7130653bbbcc6e1b82fca51e18e9acee57d398513d366` не менялся, миграций рабочих данных нет.

Осталось: полноценный браузерный исполнитель Авито без Hermes, проверенный в фоне и после перезапуска; широкий набор материалов/работ/регионов, ЕИС и OCR. Интерактивная доступность на ПК не равна готовому круглосуточному сбору. Накладные, доставка, резерв и налоги остаются явными условиями конкретного тендера.


## A3/A5 — поиск по каталогам без Авито, опубликован 19 сентября 2026

Указание: реализовать браузерный поиск без Авито и Hermes. Исходные HEAD: CRM `4f2f6af5279292ac6976ba478f3abd9839d89106`, AutoBot `28fa0035f1db9d504902a0cc81a459869109dfa2`. Сохранены чужие `.agents/`, `.codex/` и checkpoint SHA `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8`. Работа и тестовые цены — в изолированной копии и контейнерах без рабочих смет, секретов и очередей.

Опубликован AutoBot `99afae41de8afa068a83568266ad5b19f5a40f95`, образ `sha256:eb1460c0420023f5c80cf0123adfc4ee91db57d7ebd1ac85495ef9407600cbeb`. CRM-настройка источников `6ebee4e7647c7e6cf4789845bc85a27bc6e04c83`. WebBrowserFetcher читает подключённые каталоги по теме/региону, находит настоящие ссылки одного уровня и передаёт их в общую проверку цены. Браузерные снимки переиспользуются с исходной датой, реестр входит в ключ кеша выдачи, CAPTCHA имеет общий для позиций кеш отказа на 30 минут. Лимиты времени/страниц, отмена, lease, durable acceptance, отпечаток сметы и идемпотентная публикация сохранены. Изменений схемы и миграций рабочих данных нет. Основные режимы поиска по умолчанию web; Авито включается только отдельно. Доставка/налоги/резерв не заполняются выдуманными процентами.

Реестр ограничен тремя сайтами Ярославля: beton-yar.ru, yaroslavl-beton.ru, samosval76.ru. У первого прайс на главной разрешён как известный прямой источник, цена всё равно проходит проверку страницы, марки, единицы, региона и свежести. Незарегистрированные главные страницы не приняты. В запросах явного гранитного/гравийного/известнякового/вторичного щебня сохраняются тип и фракция. М300, М 300 и латинская M300 одинаковы при сопоставлении; другая марка в локальном ценовом блоке исключается до выбора цены.

Живой проход очередь → браузер → проверка → тестовый Excel: М300, Ярославль, 29,85 с, сохранено 6 700 ₽/м³ с beton-yar.ru. Карточка того же продавца осталась кандидатом из-за слабого совпадения; независимый источник считается один раз. Для гранитного щебня 20–40 найдена правильная страница, но подтверждённой цены нет. yaroslavl-beton.ru показал CAPTCHA. Это не приёмка всех материалов/работ/регионов и не расчёт прибыли. Общий поиск с сервера: Google/Яндекс/DuckDuckGo/Brave — CAPTCHA, Startpage — отказ, Bing — статьи вместо поставщиков; Google-discovery выключен. Интерактивный браузер Codex и отдельный Chrome не эквивалентны по доступу. CAPTCHA не проходили и не обходили; продавцам не писали, формы не отправляли.

Проверки: итоговый полный AutoBot **842 passed, 2 skipped, 20 subtests passed in 137.35s** в изолированной Windows-копии с запретом внешней сети. Прежние два платформенных пропуска сохранены; Node `research_frontend_tests.js` прошёл. Новые проверки покрывают выбор региона/темы, настоящие ссылки без внешних переходов/форм, точную марку в многострочном прайсе, отложенное открытие карточки, отмену, давность снимка, паузу CAPTCHA между задачами, публикацию цены и отсутствие повторной записи, web-режим при выключенном Авито. После финального прогона поправлены только комментарии и текст CLI-справки. Самопроверка diff выполнена, независимого ревью не было. Полные CRM/Linux-наборы и раскладка 390/768/1280 не запускались; интерфейсные файлы не менялись.

Production: до выпуска проверены HEAD/образы/Nginx, отсутствие активных очередей и парсеров; backup/restore проверены для 11 SQLite, FK=0. Резерв `/opt/crm-backups/autobot-catalogs-20260919T115459Z`, rollback `crm-autobot:rollback-28fa0035f1db`, прежний Compose сохранён отдельно. GitHub с сервера не ответил за 133,7 с; коммиты, уже проверенные через origin с ПК, доставлены Git bundles по существующему SSH, проверены hash/предки и git bundle verify. Образ собран из точного коммита. Первый HTTP-запрос проверки попал в перезапуск контейнера и получил reset; после завершения запуска весь runtime-набор прошёл: 6 защищённых маршрутов, байты 7 файлов, worker lock, исходные счётчики очереди, целостность/FK 11 SQLite. CRM-образ и Nginx не изменены. Экспериментальные цены в рабочие сметы не записывались.

Следующий шаг — расширять проверенный реестр по реальным позициям и регионам, отдельно отмечая обнаруженные и подтверждённые источники. Авито/Hermes отложены. Общий интернет-поиск пока имеет описанные ограничения, широкий рынок, ЕИС и OCR остаются открытым объёмом.

## 19 сентября — проект большого модуля цен по всей смете

Новый запрос пользователя: продумать большой модуль, который ищет реальные цены по всем позициям. Исходные версии CRM `59cb3dedca8041cc8f2dd06f59a642d333024bfc`, Автобот `99afae41de8afa068a83568266ad5b19f5a40f95`. Baseline, diff и диагностические артефакты — `tmp/autobot-price-module-20260919`. Незакоммиченный checkpoint Автобота сохранён, SHA `1dd7e20d55c565b5277953c673f73a6fc357fdb2948b405e629553e77a02ebf8`; `.agents/` и `.codex/` не включены в изменения.

Подготовлена [спецификация](AUTOBOT_PRICE_MODULE_SPEC.md) с границами существующих и новых компонентов, паспортом позиции, полным планом сметы, каналами поиска, правилами доказательства цены и приёмкой A–F. Основной кандидат широкого обнаружения — официальный Yandex Search API; браузер проверяет найденные страницы. Текущие тарифы и контракт сверены с официальными источниками. Бюджет уточняется отдельным вопросом; аккаунты и платные запросы не создавались. Авито/Hermes остаются отложенными, расходы тендера задаются пользователем без произвольных процентов.

В изолированной копии кода выполнена read-only диагностика 48 локальных файлов смет, 10 229 строк. По текущему классификатору 7 287 допускают автоматический поиск, 2 942 требуют разбора. Пустая единица у 830 строк, `1.0` в колонке единицы у 1 488. Файлы до/после имеют одинаковые SHA-256. Эти значения описывают локальный набор и готовность входов по текущим правилам; они не доказывают фактический охват цен на production. Диагностические запросы подтвердили потерю фракции щебня, размера бордюра, сечения кабеля в скобках и ошибочную классификацию фасованной штукатурки. В API запуска найден предел 20 по умолчанию / 50 максимум за пакет.

Первый диагностический harness использовал неверное название колонки; исправлен по `market_analytics.COL_NAME`, сводные выгрузки исключены. Итоговый запуск прочитал 48/48 файлов без ошибок; рабочие сметы не изменялись. Прикладной код и UI не менялись; полные тесты, новые viewport-проверки и живые запросы поискового API не выполнялись. Документ проверен самостоятельно на сохранение доказательств, единиц, версий, доступа, финансовых слоёв и повторных операций; независимого ревью не было.

Следующий конкретный пакет — A: паспорт позиции и устранение потери характеристик/ошибок типа; затем B: полный план и последовательные партии всей сметы. Готовность архитектуры не означает готовность реализации или универсального поиска.
